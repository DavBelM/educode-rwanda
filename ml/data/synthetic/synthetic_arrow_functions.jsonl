{"buggy": "const calculateTotal = (price, quantity) => {\n  price * quantity;\n};\nconsole.log(calculateTotal(2500, 3));", "fixed": "const calculateTotal = (price, quantity) => {\n  return price * quantity;\n};\nconsole.log(calculateTotal(2500, 3));", "category": "arrow_functions", "subcategory": "block_body_missing_return", "explanation": "When an arrow function uses curly braces {} for its body, it's called a 'block body'. Just like a regular function, a block body does not automatically send back a value. You must use the `return` keyword to explicitly specify what value the function should output. Without `return`, the calculation runs, but the function implicitly returns `undefined`. Add `return` before `price * quantity` to fix it.", "explanation_style": "direct"}
{"buggy": "const combineNames = firstName, lastName => firstName + ' ' + lastName;\nconsole.log(combineNames('Marie', 'Uwase'));", "fixed": "const combineNames = (firstName, lastName) => firstName + ' ' + lastName;\nconsole.log(combineNames('Marie', 'Uwase'));", "category": "arrow_functions", "subcategory": "missing_parentheses_multi_params", "explanation": "Arrow functions are very flexible! If you have only one parameter, you can write it without parentheses, like `name => ...`. But as soon as your function needs two or more parameters, JavaScript requires you to wrap all of them in parentheses. The engine needs `(firstName, lastName)` to correctly understand the parameters. Adding these parentheses will resolve the syntax error.", "explanation_style": "direct"}
{"buggy": "const greetUser = name => return `Hello, ${name}!`;\nconsole.log(greetUser('Eric'));", "fixed": "const greetUser = name => `Hello, ${name}!`;\nconsole.log(greetUser('Eric'));", "category": "arrow_functions", "subcategory": "return_in_concise_body", "explanation": "Take a look at your `greetUser` function. You're using a 'concise body' for your arrow function, which means you've left out the curly braces {}. When you do this, JavaScript automatically assumes you want to return the result of the single expression. So, if the return is implicit, does it still make sense to write the `return` keyword explicitly?", "explanation_style": "socratic"}
{"buggy": "console.log(getWelcomeMessage('Aline'));\nconst getWelcomeMessage = (username) => `Welcome, ${username}!`;", "fixed": "const getWelcomeMessage = (username) => `Welcome, ${username}!`;\nconsole.log(getWelcomeMessage('Aline'));", "category": "arrow_functions", "subcategory": "not_hoisted_like_function", "explanation": "You're calling `getWelcomeMessage` before it has been declared. In JavaScript, regular function declarations (like `function myFunction() {}`) are 'hoisted' to the top of their scope, meaning you can call them anywhere. However, arrow functions assigned to `const` variables (like `const getWelcomeMessage = ...`) are not hoisted. They behave like any other `const` variable, and you can only access them after they have been declared in your code. Move the `console.log` line below the `const` declaration to fix this 'temporal dead zone' error.", "explanation_style": "direct"}
{"buggy": "const calculateDiscount = (price, percent) => {\n  price - (price * percent / 100);\n};\nconsole.log(calculateDiscount(10000, 10));", "fixed": "const calculateDiscount = (price, percent) => {\n  return price - (price * percent / 100);\n};\nconsole.log(calculateDiscount(10000, 10));", "category": "arrow_functions", "subcategory": "block_body_missing_return", "explanation": "You've created an arrow function with a 'block body' using curly braces {}. When you use a block body, you must explicitly tell JavaScript what value to send back using the `return` keyword. Without `return`, the calculation runs, but the function doesn't actually provide the result to the part of the code that called it, so it gives `undefined`. Add `return` before your calculation, or remove the curly braces to use a 'concise body' which implicitly returns a single expression.", "explanation_style": "direct"}
{"buggy": "const formatFullName = first, last => `${first} ${last}`.toUpperCase();\nconsole.log(formatFullName('jane', 'doe'));", "fixed": "const formatFullName = (first, last) => `${first} ${last}`.toUpperCase();\nconsole.log(formatFullName('jane', 'doe'));", "category": "arrow_functions", "subcategory": "missing_parentheses_multi_params", "explanation": "Your `formatFullName` arrow function is designed to take two parameters, `first` and `last`. When an arrow function has two or more parameters, what syntax does JavaScript require around those parameters?", "explanation_style": "socratic"}
{"buggy": "const calculateSquare = number => return number * number;\nconsole.log(calculateSquare(7));", "fixed": "const calculateSquare = number => number * number;\nconsole.log(calculateSquare(7));", "category": "arrow_functions", "subcategory": "return_in_concise_body", "explanation": "Arrow functions have a special feature called a 'concise body' when you don't use curly braces {}. In this mode, if there's only one expression, JavaScript automatically understands that you want to `return` the result of that expression. So, the `return` keyword becomes unnecessary and even causes a syntax error here. You can either remove the `return` keyword to keep the concise body, or add curly braces around the body and keep `return` inside them, like `number => { return number * number; }`.", "explanation_style": "direct"}
{"buggy": "displayCurrentDate();\nconst displayCurrentDate = () => {\n  const today = new Date();\n  console.log(`Today is: ${today.toDateString()}`);\n};\n", "fixed": "const displayCurrentDate = () => {\n  const today = new Date();\n  console.log(`Today is: ${today.toDateString()}`);\n};\ndisplayCurrentDate();\n", "category": "arrow_functions", "subcategory": "not_hoisted_like_function", "explanation": "You are trying to call `displayCurrentDate()` before it has been defined. In JavaScript, `const` declarations (which you use for your arrow function) are not 'hoisted' to the top of their scope like traditional `function` declarations. This means you must define the function using `const` before you try to use it. If you try to access it too early, JavaScript gives a `ReferenceError` because it's in the 'temporal dead zone'. Move the call to `displayCurrentDate()` after its `const` declaration to fix this.", "explanation_style": "direct"}
{"buggy": "const calculateAreaOfSquare = sideLength => {\n  sideLength * sideLength;\n};\nconsole.log(calculateAreaOfSquare(5));", "fixed": "const calculateAreaOfSquare = sideLength => {\n  return sideLength * sideLength;\n};\nconsole.log(calculateAreaOfSquare(5));", "category": "arrow_functions", "subcategory": "block_body_missing_return", "explanation": "You've wrapped the calculation 'sideLength * sideLength' in curly braces {}. What does JavaScript usually expect inside curly braces in a function? Do you think it automatically knows to send the result of the last line back, or does it need a special word to tell it what to 'return' from that block of code?", "explanation_style": "socratic"}
{"buggy": "const sumTwoNumbers = num1, num2 => num1 + num2;\nconsole.log(sumTwoNumbers(10, 20));", "fixed": "const sumTwoNumbers = (num1, num2) => num1 + num2;\nconsole.log(sumTwoNumbers(10, 20));", "category": "arrow_functions", "subcategory": "missing_parentheses_multi_params", "explanation": "Arrow functions have a special rule for parameters. If there's only one parameter, you can write it without parentheses, like `n => n * 2`. But if your function takes two or more parameters, you must put parentheses around them. JavaScript sees `num1, num2` and expects `(num1, num2)`. Add parentheses around `num1, num2` to fix this syntax error.", "explanation_style": "direct"}
{"buggy": "const incrementCounter = currentCount => return currentCount + 1;\nconsole.log(incrementCounter(5));", "fixed": "const incrementCounter = currentCount => currentCount + 1;\nconsole.log(incrementCounter(5));", "category": "arrow_functions", "subcategory": "return_in_concise_body", "explanation": "You've written `currentCount => return currentCount + 1`. Arrow functions have two main ways to return a value: a 'concise body' (no curly braces) where the result of the expression is automatically returned, or a 'block body' (with curly braces) where you must use the `return` keyword. Does it make sense to use the `return` keyword when you don't have a block body (the curly braces)? How can you simplify this to make it valid JavaScript?", "explanation_style": "socratic"}
{"buggy": "console.log(sendWelcomeMessage('Faustin'));\nconst sendWelcomeMessage = studentName => {\n  return `Muraho ${studentName}, welcome to our class!`;\n};", "fixed": "const sendWelcomeMessage = studentName => {\n  return `Muraho ${studentName}, welcome to our class!`;\n};\nconsole.log(sendWelcomeMessage('Faustin'));", "category": "arrow_functions", "subcategory": "not_hoisted_like_function", "explanation": "You've tried to call `sendWelcomeMessage` before it's been declared. In JavaScript, regular `function` declarations are 'hoisted' to the top of their scope, meaning you can call them before their code appears. However, arrow functions assigned to `const` variables are not hoisted in the same way. They are like any other `const` variable and can only be accessed after they are declared. Move the call to `sendWelcomeMessage` after its `const` declaration.", "explanation_style": "direct"}
{"buggy": "const convertToDecimal = percentageValue => {\n  percentageValue / 100;\n};\nconsole.log(convertToDecimal(75));", "fixed": "const convertToDecimal = percentageValue => {\n  return percentageValue / 100;\n};\nconsole.log(convertToDecimal(75));", "category": "arrow_functions", "subcategory": "block_body_missing_return", "explanation": "When you use curly braces {} in an arrow function, it creates a 'block body'. Inside a block body, you need to use the `return` keyword to explicitly send a value back from the function. Without `return`, the function will execute the code inside the block but will silently return `undefined`. Add `return` before `percentageValue / 100` to make the function give back the correct decimal value.", "explanation_style": "direct"}
{"buggy": "const formatReceiptItem = itemName, itemPrice => `${itemName} costs ${itemPrice} FRW`;\nconsole.log(formatReceiptItem('Pen', 300));", "fixed": "const formatReceiptItem = (itemName, itemPrice) => `${itemName} costs ${itemPrice} FRW`;\nconsole.log(formatReceiptItem('Pen', 300));", "category": "arrow_functions", "subcategory": "missing_parentheses_multi_params", "explanation": "You're defining an arrow function with two parameters: `itemName` and `itemPrice`. Think about how you would define a function with multiple parameters using the traditional `function` keyword, or even an arrow function with just one parameter. What's the common syntax rule for grouping multiple parameters together in JavaScript?", "explanation_style": "socratic"}
{"buggy": "const doubleNumber = num => return num * 2;\nconsole.log(doubleNumber(7));", "fixed": "const doubleNumber = num => num * 2;\nconsole.log(doubleNumber(7));", "category": "arrow_functions", "subcategory": "return_in_concise_body", "explanation": "Arrow functions have two main forms: a 'block body' (with curly braces `{}`) where you write `return` explicitly, and a 'concise body' (without curly braces) where the value of the single expression is automatically returned. You've used the concise body form (`num => num * 2`) but still included the `return` keyword. This creates a syntax error because `return` is only allowed inside a block body. For a concise body, just remove `return` as the function will automatically return `num * 2`.", "explanation_style": "direct"}
{"buggy": "console.log(calculatePriceWithVAT(1000, 0.18));\nconst calculatePriceWithVAT = (amount, vatRate) => amount * (1 + vatRate);", "fixed": "const calculatePriceWithVAT = (amount, vatRate) => amount * (1 + vatRate);\nconsole.log(calculatePriceWithVAT(1000, 0.18));", "category": "arrow_functions", "subcategory": "not_hoisted_like_function", "explanation": "You are trying to use `calculatePriceWithVAT` before it has been declared. In JavaScript, regular function declarations (like `function myFunction() { ... }`) are 'hoisted' to the top of their scope, meaning you can call them before they appear in the code. However, arrow functions stored in `const` variables are not hoisted. They behave like any other `const` variable — you must declare them before you can use them. Move the `console.log` line to after the `const calculatePriceWithVAT` line to fix this `ReferenceError`.", "explanation_style": "direct"}
{"buggy": "const calculateAge = (birthYear, currentYear) => {\n  currentYear - birthYear;\n};\nconsole.log(calculateAge(2005, 2023));", "fixed": "const calculateAge = (birthYear, currentYear) => {\n  return currentYear - birthYear;\n};\nconsole.log(calculateAge(2005, 2023));", "category": "arrow_functions", "subcategory": "block_body_missing_return", "explanation": "You have written an arrow function with curly braces `{}`. What does JavaScript call this type of function body? When you use curly braces, do you remember if you need a special keyword to send a value back from the function, or does it return the value automatically like in a single-line arrow function?", "explanation_style": "socratic"}
{"buggy": "const concatenateStrings = part1, part2 => part1 + ' ' + part2;\nconsole.log(concatenateStrings('Hello', 'World'));", "fixed": "const concatenateStrings = (part1, part2) => part1 + ' ' + part2;\nconsole.log(concatenateStrings('Hello', 'World'));", "category": "arrow_functions", "subcategory": "missing_parentheses_multi_params", "explanation": "When an arrow function takes a single parameter, you can write it without parentheses, like `name => name.toUpperCase()`. But as soon as you have two or more parameters, like `part1` and `part2` here, JavaScript requires you to wrap all of them in parentheses: `(part1, part2)`. Without these parentheses, the code is invalid, causing a `SyntaxError`. Add parentheses around `part1, part2` to fix this.", "explanation_style": "direct"}
{"buggy": "const multiplyNumbers = (num1, num2) => return num1 * num2;\nconsole.log(multiplyNumbers(8, 5));", "fixed": "const multiplyNumbers = (num1, num2) => num1 * num2;\nconsole.log(multiplyNumbers(8, 5));", "category": "arrow_functions", "subcategory": "return_in_concise_body", "explanation": "You've written an arrow function that aims to return a value directly, without using curly braces `{}` for its body. What is this type of arrow function body called? In this type of body, is the `return` keyword needed, or does JavaScript implicitly return the result of the expression?", "explanation_style": "socratic"}
{"buggy": "console.log(displayMessage('Welcome to our app!')); const displayMessage = (message) => { return `MESSAGE: ${message}`; };", "fixed": "const displayMessage = (message) => { return `MESSAGE: ${message}`; }; console.log(displayMessage('Welcome to our app!'));", "category": "arrow_functions", "subcategory": "not_hoisted_like_function", "explanation": "In JavaScript, regular function declarations (like `function myFunction() {}`) are 'hoisted,' meaning you can call them before they appear in your code. However, arrow functions assigned to `const` variables are not hoisted. You must define them before you try to use them. Moving the `console.log` call after the `const displayMessage` line will fix this `ReferenceError`.", "explanation_style": "direct"}
{"buggy": "const calculatePercentage = (value, total) => { (value / total) * 100; }; console.log(calculatePercentage(25, 100));", "fixed": "const calculatePercentage = (value, total) => { return (value / total) * 100; }; console.log(calculatePercentage(25, 100));", "category": "arrow_functions", "subcategory": "block_body_missing_return", "explanation": "When an arrow function uses curly braces `{}` to define its body (this is called a 'block body'), it acts like a regular function. For the function to send back a value, you must explicitly use the `return` keyword inside the block. Without `return`, the calculation happens, but the result is not passed back, so the function implicitly returns `undefined`. Add `return` before `(value / total) * 100`.", "explanation_style": "direct"}
{"buggy": "const formatDate = day, month, year => `${day}/${month}/${year}`; console.log(formatDate(15, 8, 2024));", "fixed": "const formatDate = (day, month, year) => `${day}/${month}/${year}`; console.log(formatDate(15, 8, 2024));", "category": "arrow_functions", "subcategory": "missing_parentheses_multi_params", "explanation": "You've created an arrow function `formatDate` to combine day, month, and year. Arrow functions sometimes let you omit parentheses around parameters, but is that always true, especially when you have more than one parameter?", "explanation_style": "socratic"}
{"buggy": "const calculateDiscountAmount = amount => return amount * 0.10; console.log(calculateDiscountAmount(25000));", "fixed": "const calculateDiscountAmount = amount => amount * 0.10; console.log(calculateDiscountAmount(25000));", "category": "arrow_functions", "subcategory": "return_in_concise_body", "explanation": "Arrow functions have two main forms: a 'concise body' (no curly braces `{}`) or a 'block body' (with curly braces `{}`). With a concise body, the expression after `=>` is automatically returned, so you don't need the `return` keyword. The `return` keyword is only used inside a 'block body'. To fix this, either remove the `return` keyword or add curly braces around the function body: `amount => { return amount * 0.10; }`.", "explanation_style": "direct"}
{"buggy": "console.log(displayGreeting('Alice'));\nconst displayGreeting = (name) => {\n  return `Muraho, ${name}!`;\n};", "fixed": "const displayGreeting = (name) => {\n  return `Muraho, ${name}!`;\n};\nconsole.log(displayGreeting('Alice'));", "category": "arrow_functions", "subcategory": "not_hoisted_like_function", "explanation": "In JavaScript, regular `function` declarations are 'hoisted', meaning they are moved to the top of their scope before code execution. This allows you to call them before they are defined. However, arrow functions assigned to `const` variables are not hoisted in the same way. They behave like any other `const` variable, meaning you cannot access them before their declaration. This causes a `ReferenceError` because `displayGreeting` has not been initialized yet when you try to call it. To fix this, always define your `const` arrow functions before you try to use them.", "explanation_style": "direct"}
{"buggy": "const calculateTotalWithTax = (price, taxRate) => {\n  const total = price + (price * taxRate);\n};\nconsole.log(calculateTotalWithTax(1000, 0.18));", "fixed": "const calculateTotalWithTax = (price, taxRate) => {\n  const total = price + (price * taxRate);\n  return total;\n};\nconsole.log(calculateTotalWithTax(1000, 0.18));", "category": "arrow_functions", "subcategory": "block_body_missing_return", "explanation": "Take a close look at the curly braces `{}` around the code inside your arrow function. When you use these curly braces, you're creating a 'block body'. Does JavaScript automatically know that the last calculation you perform inside this block (the `total` value) is what you want the function to give back? Or does a block body usually need a special word to tell it what to 'return'?", "explanation_style": "socratic"}
{"buggy": "const joinNames = firstName, lastName => {\n  return `${firstName} ${lastName}`;\n};\nconsole.log(joinNames('Mugisha', 'Eric'));", "fixed": "const joinNames = (firstName, lastName) => {\n  return `${firstName} ${lastName}`;\n};\nconsole.log(joinNames('Mugisha', 'Eric'));", "category": "arrow_functions", "subcategory": "missing_parentheses_multi_params", "explanation": "Arrow functions have a special rule for parameters. If there is only one parameter (e.g., `name => ...`), you can leave out the parentheses. But if you have zero parameters (`() => ...`) or two or more parameters (e.g., `firstName, lastName`), you *must* wrap all the parameters in parentheses. JavaScript sees `firstName, lastName => ...` and doesn't understand it as valid syntax for an arrow function, leading to a `SyntaxError`. Adding `()` around `firstName, lastName` will solve this.", "explanation_style": "direct"}
{"buggy": "const calculateCube = num => return num * num * num;\nconsole.log(calculateCube(3));", "fixed": "const calculateCube = num => num * num * num;\nconsole.log(calculateCube(3));", "category": "arrow_functions", "subcategory": "return_in_concise_body", "explanation": "Look at your `calculateCube` function. You've written `num => return num * num * num`. When an arrow function uses a 'concise body' (meaning there are no curly braces `{}` after the `=>`), it automatically returns the result of the expression that follows. Do you think you still need the `return` keyword in this situation, or does JavaScript handle the returning for you?", "explanation_style": "socratic"}
{"buggy": "console.log(formatDateString(new Date()));\nconst formatDateString = date => `${date.getFullYear()}-${(date.getMonth() + 1).toString().padStart(2, '0')}-${date.getDate().toString().padStart(2, '0')}`;", "fixed": "const formatDateString = date => `${date.getFullYear()}-${(date.getMonth() + 1).toString().padStart(2, '0')}-${date.getDate().toString().padStart(2, '0')}`;\nconsole.log(formatDateString(new Date()));", "category": "arrow_functions", "subcategory": "not_hoisted_like_function", "explanation": "You've called `formatDateString` before it's declared with `const`. While regular `function` declarations are 'hoisted' (meaning JavaScript moves them to the top of their scope so you can call them anywhere), arrow functions assigned to `const` variables are not. They are like any other `const` variable and must be declared before they are accessed. To fix this, simply move the `console.log()` call after the `const formatDateString` declaration.", "explanation_style": "direct"}
{"buggy": "const getDiscountAmount = (price, discountPercent) => {\n  price * (discountPercent / 100);\n};\nconsole.log(getDiscountAmount(1000, 10));", "fixed": "const getDiscountAmount = (price, discountPercent) => {\n  return price * (discountPercent / 100);\n};\nconsole.log(getDiscountAmount(1000, 10));", "category": "arrow_functions", "subcategory": "block_body_missing_return", "explanation": "Your `getDiscountAmount` function is using curly braces `{}` to define its body. When an arrow function has a 'block body' like this, it acts just like a regular function: you must explicitly use the `return` keyword if you want the function to send back a value. Without `return`, the calculation happens, but the function silently returns `undefined`. Add `return` before your calculation to fix this.", "explanation_style": "direct"}
{"buggy": "const calculateSum = num1, num2 => num1 + num2;\nconsole.log(calculateSum(10, 20));", "fixed": "const calculateSum = (num1, num2) => num1 + num2;\nconsole.log(calculateSum(10, 20));", "category": "arrow_functions", "subcategory": "missing_parentheses_multi_params", "explanation": "Look closely at how you've defined the parameters for `calculateSum`. When an arrow function takes *multiple* parameters, how does JavaScript usually expect them to be grouped together?", "explanation_style": "socratic"}
{"buggy": "const getDouble = number => return number * 2;\nconsole.log(getDouble(5));", "fixed": "const getDouble = number => number * 2;\nconsole.log(getDouble(5));", "category": "arrow_functions", "subcategory": "return_in_concise_body", "explanation": "You're trying to use the `return` keyword in a concise arrow function body. A 'concise body' means you omit the curly braces `{}` and the `return` keyword entirely; JavaScript automatically returns the result of the single expression. If you want to use the `return` keyword, you must use a 'block body' by adding curly braces around your code: `number => { return number * 2; }`. For this simple case, removing `return` is often cleaner: `number => number * 2`.", "explanation_style": "direct"}
{"buggy": "console.log(displayWelcome('Jean'));\nconst displayWelcome = name => `Muraho, ${name}! Welcome to the system.`;", "fixed": "const displayWelcome = name => `Muraho, ${name}! Welcome to the system.`;\nconsole.log(displayWelcome('Jean'));", "category": "arrow_functions", "subcategory": "not_hoisted_like_function", "explanation": "In JavaScript, regular function declarations (like `function myFunction() { ... }`) are 'hoisted' to the top of their scope. This means you can call them before they are written in your code. However, arrow functions assigned to `const` variables, like `displayWelcome` here, are not hoisted in the same way. You must declare them before you try to call them. Move the `console.log` line after the `const displayWelcome` line, and your code will work.", "explanation_style": "direct"}
{"buggy": "const calculateVAT = (amount) => {\n  amount * 0.18;\n};\nconsole.log(calculateVAT(1000));", "fixed": "const calculateVAT = (amount) => {\n  return amount * 0.18;\n};\nconsole.log(calculateVAT(1000));", "category": "arrow_functions", "subcategory": "block_body_missing_return", "explanation": "Look at the curly braces `{}` around `amount * 0.18`. When an arrow function uses these curly braces, JavaScript treats it as a 'block body' — just like the body of a regular function. Does JavaScript automatically know that you want to send the result of `amount * 0.18` back to whoever called the function when it's inside a block body, or does it need a special word to tell it to do so?", "explanation_style": "socratic"}
{"buggy": "const calculateNetSalary = grossSalary, deductions => grossSalary - deductions;\nconsole.log(calculateNetSalary(100000, 15000));", "fixed": "const calculateNetSalary = (grossSalary, deductions) => grossSalary - deductions;\nconsole.log(calculateNetSalary(100000, 15000));", "category": "arrow_functions", "subcategory": "missing_parentheses_multi_params", "explanation": "You're creating an arrow function with two parameters: `grossSalary` and `deductions`. When an arrow function takes more than one parameter, you must always wrap those parameters in parentheses `()`. For example, `(param1, param2) => ...`. If there's only one parameter, you can sometimes skip the parentheses (e.g., `name => ...`), but it's always safe to include them. Add parentheses around `grossSalary, deductions` to fix this syntax error.", "explanation_style": "direct"}
{"buggy": "const calculateAverage = (num1, num2) => return (num1 + num2) / 2;\nconsole.log(calculateAverage(10, 20));", "fixed": "const calculateAverage = (num1, num2) => (num1 + num2) / 2;\nconsole.log(calculateAverage(10, 20));", "category": "arrow_functions", "subcategory": "return_in_concise_body", "explanation": "You've written `return` inside an arrow function without curly braces. When an arrow function like `num => num + 1` has no curly braces, how does it know what value to send back? Does it need the `return` keyword in that situation, or is it already 'implicitly returning' the result of the expression?", "explanation_style": "socratic"}
{"buggy": "console.log(displayProductInfo('Sugar', 5));\n\nconst displayProductInfo = (productName, quantity) => {\n  return `You have ${quantity} units of ${productName}.`;\n};", "fixed": "const displayProductInfo = (productName, quantity) => {\n  return `You have ${quantity} units of ${productName}.`;\n};\n\nconsole.log(displayProductInfo('Sugar', 5));", "category": "arrow_functions", "subcategory": "not_hoisted_like_function", "explanation": "You are trying to call the `displayProductInfo` function before it has been declared. While regular `function` declarations are 'hoisted' (meaning they can be called before their definition in the code), arrow functions assigned to `const` variables are not. They behave like any other variable declared with `const` — you must define them before you use them. Move the `console.log` line to appear after the `const displayProductInfo` declaration.", "explanation_style": "direct"}
{"buggy": "const getTotalPrice = (unitPrice, quantity) => {\n  unitPrice * quantity;\n};\nconsole.log(getTotalPrice(500, 3));", "fixed": "const getTotalPrice = (unitPrice, quantity) => unitPrice * quantity;\nconsole.log(getTotalPrice(500, 3));", "category": "arrow_functions", "subcategory": "block_body_missing_return", "explanation": "When an arrow function uses curly braces `{}` for its body, it's called a 'block body'. Just like with regular functions, if you want the block body to send back a value, you *must* use the `return` keyword. If you don't, the function will run the code inside, but then silently return `undefined`. To fix this, you can either add `return` before `unitPrice * quantity` inside the braces, or a common good practice is to remove the braces entirely if your function body is just a single expression, because then the result is automatically returned. This is called a 'concise body'.", "explanation_style": "direct"}
{"buggy": "const calculateRectangleArea = width, height => width * height;\nconsole.log(calculateRectangleArea(10, 5));", "fixed": "const calculateRectangleArea = (width, height) => width * height;\nconsole.log(calculateRectangleArea(10, 5));", "category": "arrow_functions", "subcategory": "missing_parentheses_multi_params", "explanation": "You've defined an arrow function `calculateRectangleArea` to take two parameters: `width` and `height`. Can you recall the rule for how to define multiple parameters in an arrow function's declaration? What kind of brackets or parentheses does JavaScript expect around multiple parameters?", "explanation_style": "socratic"}
{"buggy": "const getStudentDetails = (name, id) => {\n  studentName: name, studentId: id\n};\nconsole.log(getStudentDetails(\"Alice\", 101));", "fixed": "const getStudentDetails = (name, id) => ({\n  studentName: name, studentId: id\n});\nconsole.log(getStudentDetails(\"Alice\", 101));", "category": "arrow_functions", "subcategory": "implicit_return_object_literal", "explanation": "When an arrow function uses curly braces {} without the 'return' keyword, JavaScript expects a block of code containing statements, not an object literal to be returned implicitly. The code inside the braces, like 'studentName: name', is interpreted as a labeled statement. The comma between properties then causes a SyntaxError because it's not expected in that context. To implicitly return an object literal, you must wrap the entire object literal in parentheses, like `({ key: value })`.", "explanation_style": "direct"}
{"buggy": "const createProduct = (productName, price) => {\n  name: productName, price: price, available: true\n};\nconsole.log(createProduct(\"Laptop\", 850000));", "fixed": "const createProduct = (productName, price) => ({\n  name: productName, price: price, available: true\n});\nconsole.log(createProduct(\"Laptop\", 850000));", "category": "arrow_functions", "subcategory": "implicit_return_object_literal", "explanation": "If you want an arrow function to automatically return an object literal without using the 'return' keyword, you cannot just wrap the object in curly braces directly. JavaScript thinks those outer curly braces define a 'block body' for the function. Inside a block body, 'name: productName' is seen as a statement label, and the subsequent comma causes a syntax error. To implicitly return an object, you need to put parentheses around the object literal itself: `({ property: value })`.", "explanation_style": "direct"}
{"buggy": "const getTaskInfo = (title, completed) => {\n  taskTitle: title, isCompleted: completed\n};\nconsole.log(getTaskInfo(\"Learn JavaScript\", false));", "fixed": "const getTaskInfo = (title, completed) => ({\n  taskTitle: title, isCompleted: completed\n});\nconsole.log(getTaskInfo(\"Learn JavaScript\", false));", "category": "arrow_functions", "subcategory": "implicit_return_object_literal", "explanation": "When an arrow function's body is enclosed in curly braces {}, JavaScript interprets it as a 'block body' where you typically have multiple statements, and you need an explicit 'return' statement to send a value back. If you try to place an object literal directly inside these braces for an implicit return, JavaScript gets confused. It treats 'taskTitle: title' as a labeled statement, and the comma after it creates a SyntaxError. To make it implicitly return an object, wrap the object literal in parentheses.", "explanation_style": "direct"}
{"buggy": "const createUserProfile = (username, email) => {\n  userName: username, userEmail: email\n};\nconsole.log(createUserProfile(\"kigali_coder\", \"kigali@example.com\"));", "fixed": "const createUserProfile = (username, email) => ({\n  userName: username, userEmail: email\n});\nconsole.log(createUserProfile(\"kigali_coder\", \"kigali@example.com\"));", "category": "arrow_functions", "subcategory": "implicit_return_object_literal", "explanation": "An arrow function can implicitly return a value without the 'return' keyword if its body is a single expression (without curly braces, like `() => 1 + 1`). However, if you add curly braces, JavaScript expects a block of code. When you try to implicitly return an object literal like `{ userName: username, userEmail: email }` inside these braces, it fails. The engine sees `userName:` as a label, and the comma creates a syntax error. To implicitly return an object literal, you must wrap it in parentheses: `() => ({ key: value })`.", "explanation_style": "direct"}
{"buggy": "const getItemData = (id, stock) => {\n  itemId: id, currentStock: stock, location: \"Warehouse\"\n};\nconsole.log(getItemData(\"SKU001\", 50));", "fixed": "const getItemData = (id, stock) => ({\n  itemId: id, currentStock: stock, location: \"Warehouse\"\n});\nconsole.log(getItemData(\"SKU001\", 50));", "category": "arrow_functions", "subcategory": "implicit_return_object_literal", "explanation": "When you use curly braces `{}` in an arrow function, it's considered a 'block body' where you would normally put statements and an explicit `return` keyword. JavaScript does not implicitly return the last expression in a block body. If you try to put an object literal directly in a block body for implicit return, JavaScript interprets `itemId: id` as a label for a statement, and the comma after it causes a `SyntaxError`. To correctly implicitly return an object literal, wrap it in parentheses: `() => ({ property: value })`.", "explanation_style": "direct"}
{"buggy": "const formatAddress = (street, city) => {\n  streetName: street, cityName: city, country: \"Rwanda\"\n};\nconsole.log(formatAddress(\"KG 123 St\", \"Kigali\"));", "fixed": "const formatAddress = (street, city) => ({\n  streetName: street, cityName: city, country: \"Rwanda\"\n});\nconsole.log(formatAddress(\"KG 123 St\", \"Kigali\"));", "category": "arrow_functions", "subcategory": "implicit_return_object_literal", "explanation": "An arrow function uses curly braces `{}` to define a 'block body' for multiple statements. If you want to implicitly return an object literal, you can't just put `{ key: value, ... }` directly after the arrow. JavaScript will interpret 'streetName: street' as a label, and the comma that follows it will result in a SyntaxError. To make it work, you must enclose the object literal within parentheses: `() => ({ key: value, ... })`.", "explanation_style": "direct"}
{"buggy": "const getPaymentInfo = (amount, currency) => {\n  paymentAmount: amount, paymentCurrency: currency\n};\nconsole.log(getPaymentInfo(1500, \"RWF\"));", "fixed": "const getPaymentInfo = (amount, currency) => ({\n  paymentAmount: amount, paymentCurrency: currency\n});\nconsole.log(getPaymentInfo(1500, \"RWF\"));", "category": "arrow_functions", "subcategory": "implicit_return_object_literal", "explanation": "When an arrow function has a 'concise body' (no curly braces), it implicitly returns the result of the expression. But when you add curly braces `{}`, you're creating a 'block body'. Inside a block body, simply listing object properties like `paymentAmount: amount` is seen as a labeled statement, not an object literal to be returned. The comma after the first property causes a SyntaxError. To implicitly return an object from a concise body, you must wrap the object literal in parentheses: `() => ({ key: value })`.", "explanation_style": "direct"}
{"buggy": "const createConfig = (theme, language) => {\n  appTheme: theme, appLanguage: language\n};\nconsole.log(createConfig(\"dark\", \"kinyarwanda\"));", "fixed": "const createConfig = (theme, language) => ({\n  appTheme: theme, appLanguage: language\n});\nconsole.log(createConfig(\"dark\", \"kinyarwanda\"));", "category": "arrow_functions", "subcategory": "implicit_return_object_literal", "explanation": "An arrow function with curly braces `{}` is considered a 'block body', which expects statements to be executed, and requires an explicit 'return' keyword to send a value back. When you put an object literal directly inside these braces for an implicit return, JavaScript interprets `appTheme: theme` as a labeled statement. The subsequent comma leads to a SyntaxError because it's not valid syntax in that context. To make it work, wrap the object literal in parentheses: `() => ({ key: value })`.", "explanation_style": "direct"}
{"buggy": "const getUserStatus = (id, online) => {\n  userId: id, isOnline: online\n};\nconsole.log(getUserStatus(789, true));", "fixed": "const getUserStatus = (id, online) => ({\n  userId: id, isOnline: online\n});\nconsole.log(getUserStatus(789, true));", "category": "arrow_functions", "subcategory": "implicit_return_object_literal", "explanation": "Arrow functions can implicitly return an expression. However, if that expression is an object literal starting with curly braces `{}`, and you also use curly braces `()` for the function's block body, JavaScript gets confused. It interprets `userId: id` as a label for a statement, and the comma `isOnline: online` causes a SyntaxError. To make it clear you want to return an object implicitly, you need to wrap the object literal itself in parentheses: `() => ({ key: value })`.", "explanation_style": "direct"}
{"buggy": "const getWeatherData = (city, temp) => {\n  cityName: city, temperature: temp, unit: \"Celsius\"\n};\nconsole.log(getWeatherData(\"Huye\", 25));", "fixed": "const getWeatherData = (city, temp) => ({\n  cityName: city, temperature: temp, unit: \"Celsius\"\n});\nconsole.log(getWeatherData(\"Huye\", 25));", "category": "arrow_functions", "subcategory": "implicit_return_object_literal", "explanation": "When an arrow function has a 'block body' (curly braces {}), it requires an explicit 'return' statement to send a value. If you try to implicitly return an object literal by simply putting it inside the block, JavaScript interprets `cityName: city` as a labeled statement, not an object. The comma between `temperature: temp` then causes a SyntaxError. To correctly implicitly return an object literal, you must wrap it in parentheses: `() => ({ property: value })`.", "explanation_style": "direct"}
{"buggy": "const squareNumber = num => return num * num;\nconsole.log(squareNumber(5));", "fixed": "const squareNumber = num => num * num;\nconsole.log(squareNumber(5));", "category": "arrow_functions", "subcategory": "return_in_concise_body", "explanation": "Arrow functions have two ways to define their body: a 'concise body' (without curly braces) or a 'block body' (with curly braces {}). When you use a concise body like num => num * num, JavaScript automatically understands that the result of the expression num * num should be returned. Adding the return keyword in a concise body is not allowed and causes a SyntaxError. To fix this, just remove the return keyword.", "explanation_style": "direct"}
{"buggy": "console.log(getGreeting('Alice'));\nconst getGreeting = name => `Hello, ${name}!`;", "fixed": "const getGreeting = name => `Hello, ${name}!`;\nconsole.log(getGreeting('Alice'));", "category": "arrow_functions", "subcategory": "not_hoisted_like_function", "explanation": "You might be used to writing regular function declarations (like function greet() {}) and calling them from anywhere in your code, even before they are defined. This is because JavaScript 'hoists' regular function declarations to the top. However, arrow functions stored in a const variable (like const getGreeting = ...) are not hoisted. You must define them before you try to use them. Moving the call after the declaration fixes this 'ReferenceError'.", "explanation_style": "direct"}
{"buggy": "const calculateTotal = (price, quantity) => {\n  price * quantity;\n};\nconsole.log(calculateTotal(100, 3));", "fixed": "const calculateTotal = (price, quantity) => {\n  return price * quantity;\n};\nconsole.log(calculateTotal(100, 3));", "category": "arrow_functions", "subcategory": "block_body_missing_return", "explanation": "You've used curly braces {} around price * quantity. What does JavaScript usually expect when you have a block of code like that? Does it automatically know you want to send the result of price * quantity back to whoever called the function, or do you need to give it a specific instruction?", "explanation_style": "socratic"}
{"buggy": "const formatName = first, last => first.toUpperCase() + ' ' + last.toUpperCase();\nconsole.log(formatName('John', 'Doe'));", "fixed": "const formatName = (first, last) => first.toUpperCase() + ' ' + last.toUpperCase();\nconsole.log(formatName('John', 'Doe'));", "category": "arrow_functions", "subcategory": "missing_parentheses_multi_params", "explanation": "When an arrow function takes only one parameter, you can often leave out the parentheses around it, like `name => name.length`. However, when your arrow function needs two or more parameters, like first and last here, JavaScript requires you to wrap all the parameters in parentheses. The code `first, last => ...` causes a SyntaxError because JavaScript doesn't understand it as a list of parameters. Adding parentheses around `first, last` will fix this.", "explanation_style": "direct"}
{"buggy": "const getUserData = () => {\n  id: 101,\n  username: 'student_alpha'\n};\nconsole.log(getUserData());", "fixed": "const getUserData = () => ({\n  id: 101,\n  username: 'student_alpha'\n});\nconsole.log(getUserData());", "category": "arrow_functions", "subcategory": "implicit_return_object_literal", "explanation": "When you want an arrow function to directly return an object literal using the 'concise body' syntax (without an explicit return keyword), you need to wrap the object literal in parentheses. Without the parentheses, JavaScript sees the curly braces {} and thinks you're starting a block of code, not defining an object to be returned. Then, when it sees `id: 101`, it interprets `id:` as a label, and the comma after `101` causes a SyntaxError because it's not expecting an expression there. Wrapping the object in parentheses `({})` tells JavaScript, 'This is an object I want to return, not a code block.'", "explanation_style": "direct"}
{"buggy": "const multiplyValues = (a, b) => return a * b;\nconsole.log(multiplyValues(7, 8));", "fixed": "const multiplyValues = (a, b) => a * b;\nconsole.log(multiplyValues(7, 8));", "category": "arrow_functions", "subcategory": "return_in_concise_body", "explanation": "You're trying to return the result of a * b. Arrow functions can either use a 'block body' (with curly braces and an explicit return) or a 'concise body' (without curly braces, for a single expression). If you're using the concise body, what does JavaScript do automatically with the result of the expression you write? Do you still need to type the return keyword?", "explanation_style": "socratic"}
{"buggy": "console.log(sayGoodbye('Fiona'));\nconst sayGoodbye = name => `Goodbye, ${name}!`;", "fixed": "const sayGoodbye = name => `Goodbye, ${name}!`;\nconsole.log(sayGoodbye('Fiona'));", "category": "arrow_functions", "subcategory": "not_hoisted_like_function", "explanation": "JavaScript treats 'function declarations' (like `function myFunction() {}`) differently from functions assigned to variables with `const`, `let`, or `var`. Regular function declarations are 'hoisted,' meaning they are available throughout their scope, even before their actual definition in the code. However, an arrow function defined with `const` (like `const sayGoodbye = ...`) is not hoisted. It's stored in a variable, and just like any other `const` variable, you cannot access it before its line of code is executed. This is called the 'temporal dead zone.' To fix this, simply move the `console.log` call after the `sayGoodbye` function is defined.", "explanation_style": "direct"}
{"buggy": "const calculateTax = (amount) => {\n  amount * 0.18;\n};\nconst price = 5000;\nconst taxAmount = calculateTax(price);\nconsole.log(`Tax for ${price} is: ${taxAmount}`);", "fixed": "const calculateTax = (amount) => {\n  return amount * 0.18;\n};\nconst price = 5000;\nconst taxAmount = calculateTax(price);\nconsole.log(`Tax for ${price} is: ${taxAmount}`);", "category": "arrow_functions", "subcategory": "block_body_missing_return", "explanation": "When you use curly braces {} in an arrow function, it creates a 'block body.' In a block body, you must explicitly use the `return` keyword to send a value back from the function. If you don't include `return`, the expression (like `amount * 0.18`) is computed, but its result is not sent out of the function. JavaScript then implicitly returns `undefined`. To make `calculateTax` return the correct tax amount, just add `return` before `amount * 0.18`.", "explanation_style": "direct"}
{"buggy": "const createMessage = sender, messageText => `From ${sender}: ${messageText}`;\nconsole.log(createMessage('Admin', 'Welcome!'));", "fixed": "const createMessage = (sender, messageText) => `From ${sender}: ${messageText}`;\nconsole.log(createMessage('Admin', 'Welcome!'));", "category": "arrow_functions", "subcategory": "missing_parentheses_multi_params", "explanation": "You've defined an arrow function with two parameters: sender and messageText. Think about how you would define a regular function that takes two arguments, like `function myFunc(arg1, arg2)`. What kind of grouping do you usually put around the argument list? Do arrow functions follow a similar rule when they have multiple inputs?", "explanation_style": "socratic"}
{"buggy": "const getItemDetails = () => {\n  itemName: 'Laptop',\n  price: 1200000,\n  inStock: true\n};\nconsole.log(getItemDetails());", "fixed": "const getItemDetails = () => ({\n  itemName: 'Laptop',\n  price: 1200000,\n  inStock: true\n});\nconsole.log(getItemDetails());", "category": "arrow_functions", "subcategory": "implicit_return_object_literal", "explanation": "When an arrow function has a 'concise body' (no explicit `return` keyword) and you want it to return an object literal directly, you must wrap the object literal in parentheses. If you use curly braces `{}` without the parentheses `({})`, JavaScript tries to parse it as a 'block body' of statements, not an object. When it sees `itemName: 'Laptop'`, it interprets `itemName:` as a label, and the comma after 'Laptop' then causes a SyntaxError because it's not expected in that context. Adding parentheses around the object `{}` tells JavaScript to treat it as an object to be returned.", "explanation_style": "direct"}
{"buggy": "const calculateProductPrice = (itemPrice, quantity) => return itemPrice * quantity;\nconsole.log(calculateProductPrice(1500, 3));", "fixed": "const calculateProductPrice = (itemPrice, quantity) => itemPrice * quantity;\nconsole.log(calculateProductPrice(1500, 3));", "category": "arrow_functions", "subcategory": "return_in_concise_body", "explanation": "When an arrow function has a 'concise body' (meaning no curly braces {}), it automatically returns the result of the expression. You don't need to use the return keyword. In fact, if you use 'return' without curly braces, JavaScript gets confused and throws a SyntaxError. To fix this, just remove the 'return' keyword.", "explanation_style": "direct"}
{"buggy": "console.log(greetStudent('Aline'));\n\nconst greetStudent = name => `Muraho, ${name}!`;", "fixed": "const greetStudent = name => `Muraho, ${name}!`;\n\nconsole.log(greetStudent('Aline'));", "category": "arrow_functions", "subcategory": "not_hoisted_like_function", "explanation": "You've called greetStudent before it was declared! Unlike regular function declarations (like function myFunction() {}), arrow functions stored in a 'const' variable are not 'hoisted' to the top of their scope. This means you must define them before you try to use them. Moving the 'console.log' call after the 'const greetStudent' declaration will fix this `ReferenceError`.", "explanation_style": "direct"}
{"buggy": "const processScore = score => {\n  let result = score >= 70 ? 'Distinction' : 'Pass';\n};\nconsole.log(processScore(75));", "fixed": "const processScore = score => {\n  let result = score >= 70 ? 'Distinction' : 'Pass';\n  return result;\n};\nconsole.log(processScore(75));", "category": "arrow_functions", "subcategory": "block_body_missing_return", "explanation": "Look at the curly braces around your code inside `processScore`. When an arrow function uses curly braces {}, JavaScript treats this as a 'block body', similar to a regular function. Does JavaScript automatically know that you want to send back the `result` variable from inside this block, or does it think those braces are just wrapping some statements without a specific return value?", "explanation_style": "socratic"}
{"buggy": "const sumNumbers = num1, num2 => num1 + num2;\nconsole.log(sumNumbers(25, 15));", "fixed": "const sumNumbers = (num1, num2) => num1 + num2;\nconsole.log(sumNumbers(25, 15));", "category": "arrow_functions", "subcategory": "missing_parentheses_multi_params", "explanation": "Arrow functions are flexible! If you have only one parameter, you can write `param => ...`. But if your function needs two or more parameters, JavaScript requires you to wrap them in parentheses, like `(param1, param2) => ...`. Without these parentheses, the JavaScript engine gets confused and can't understand your function definition, leading to a SyntaxError.", "explanation_style": "direct"}
{"buggy": "const getUserProfile = () => { userName: 'Keza', studentId: 'S001', grade: 12 };\nconsole.log(getUserProfile());", "fixed": "const getUserProfile = () => ({ userName: 'Keza', studentId: 'S001', grade: 12 });\nconsole.log(getUserProfile());", "category": "arrow_functions", "subcategory": "implicit_return_object_literal", "explanation": "You're trying to implicitly return an object literal using concise arrow function syntax. However, JavaScript sees the `{}` and thinks you're starting a 'block body' (a set of statements), not an object. When it encounters the colon in 'userName: 'Keza'', it causes a SyntaxError because it expects a statement, not an object property. To tell JavaScript that you want to return an object, simply wrap the object literal in parentheses: `() => ({ key: value })`. This makes the object explicit.", "explanation_style": "direct"}
{"buggy": "const calculateTotalItems = count => return count * 2;\nconsole.log(calculateTotalItems(5));", "fixed": "const calculateTotalItems = count => count * 2;\nconsole.log(calculateTotalItems(5));", "category": "arrow_functions", "subcategory": "return_in_concise_body", "explanation": "You've written an arrow function without curly braces, which is called a 'concise body' or 'implicit return'. When you use this short form, JavaScript already assumes you want to return the result of the expression. So, what happens when you try to add the 'return' keyword to something that is already implicitly returning its value?", "explanation_style": "socratic"}
{"buggy": "console.log(getItemPrice('Pen'));\n\nconst getItemPrice = item => {\n  if (item === 'Pen') return 200;\n  return 100;\n};", "fixed": "const getItemPrice = item => {\n  if (item === 'Pen') return 200;\n  return 100;\n};\n\nconsole.log(getItemPrice('Pen'));", "category": "arrow_functions", "subcategory": "not_hoisted_like_function", "explanation": "When you declare an arrow function using `const`, it behaves like any other variable declared with `const`. It's not available until its line of code is executed. This means you cannot call `getItemPrice` before its `const` declaration appears in the code. This is different from `function` declarations, which are 'hoisted' to the top. To fix this, just move the call to `getItemPrice` after its definition.", "explanation_style": "direct"}
{"buggy": "const getMessage = (userName, subject) => {\n  const fullMessage = `Dear ${userName}, regarding ${subject}.`;\n};\nconsole.log(getMessage('Claudine', 'your application'));", "fixed": "const getMessage = (userName, subject) => {\n  const fullMessage = `Dear ${userName}, regarding ${subject}.`;\n  return fullMessage;\n};\nconsole.log(getMessage('Claudine', 'your application'));", "category": "arrow_functions", "subcategory": "block_body_missing_return", "explanation": "Arrow functions with curly braces {} are like regular functions; they need an explicit `return` statement to send a value back to where the function was called. Even if you calculate a value inside the braces, without `return`, the function will silently complete and return `undefined`. Add `return fullMessage;` to make sure your message is sent back.", "explanation_style": "direct"}
{"buggy": "const calculateRectanglePerimeter = width, height => 2 * (width + height);\nconsole.log(calculateRectanglePerimeter(8, 4));", "fixed": "const calculateRectanglePerimeter = (width, height) => 2 * (width + height);\nconsole.log(calculateRectanglePerimeter(8, 4));", "category": "arrow_functions", "subcategory": "missing_parentheses_multi_params", "explanation": "You're defining an arrow function with two parameters: `width` and `height`. Think about how JavaScript understands function parameters. When there's more than one parameter, what special syntax does JavaScript expect around the list of parameters to clearly identify them?", "explanation_style": "socratic"}
{"buggy": "const getProductDetails = () => { productId: 'ABC-123', productName: 'Smartphone', price: 250000, inStock: true };\nconsole.log(getProductDetails());", "fixed": "const getProductDetails = () => ({ productId: 'ABC-123', productName: 'Smartphone', price: 250000, inStock: true });\nconsole.log(getProductDetails());", "category": "arrow_functions", "subcategory": "implicit_return_object_literal", "explanation": "When an arrow function without `return` and without curly braces is meant to return an object literal, you must wrap the object literal in parentheses. Without the parentheses, JavaScript interprets the opening `{` as the start of a function block, not an object. The code then fails because it expects statements, and properties like `productName: 'Smartphone'` aren't valid statements in that context, especially with the comma separating them.", "explanation_style": "direct"}
{"buggy": "const doubleValue = num => return num * 2;\nconsole.log(doubleValue(7));", "fixed": "const doubleValue = num => num * 2;\nconsole.log(doubleValue(7));", "category": "arrow_functions", "subcategory": "return_in_concise_body", "explanation": "When you use a concise arrow function (without curly braces {}), the value to the right of the `=>` is automatically returned. You should not use the `return` keyword in this concise form. If you want to use `return`, you must wrap the function body in curly braces, like `num => { return num * 2; }`.", "explanation_style": "direct"}
{"buggy": "console.log(sayHello('Ange'));\nconst sayHello = name => `Hello, ${name}!`;", "fixed": "const sayHello = name => `Hello, ${name}!`;\nconsole.log(sayHello('Ange'));", "category": "arrow_functions", "subcategory": "not_hoisted_like_function", "explanation": "In JavaScript, regular function declarations (like `function sayHello(name) { ... }`) are 'hoisted,' meaning they can be called before they are defined in the code. However, arrow functions assigned to `const` variables are not hoisted. You must define them before you try to use them. Moving the `console.log` line after the `sayHello` declaration will fix this.", "explanation_style": "direct"}
{"buggy": "const generateGreeting = () => {\n  'Mwaramutse';\n};\nconsole.log(generateGreeting());", "fixed": "const generateGreeting = () => {\n  return 'Mwaramutse';\n};\nconsole.log(generateGreeting());", "category": "arrow_functions", "subcategory": "block_body_missing_return", "explanation": "You've wrapped the string 'Mwaramutse' in curly braces {}. What does JavaScript usually expect to happen when you use curly braces in a function? Does it automatically send back the last value, or does it need you to explicitly tell it to return something?", "explanation_style": "socratic"}
{"buggy": "const addNumbers = num1, num2 => num1 + num2;\nconsole.log(addNumbers(5, 3));", "fixed": "const addNumbers = (num1, num2) => num1 + num2;\nconsole.log(addNumbers(5, 3));", "category": "arrow_functions", "subcategory": "missing_parentheses_multi_params", "explanation": "Arrow functions are flexible with parameters. If you have only one parameter, you can often skip the parentheses, like `item => item.price`. But when you have two or more parameters, JavaScript strictly requires you to wrap them in parentheses. The code `num1, num2 => ...` is not valid syntax. Add parentheses around `num1, num2` to fix this.", "explanation_style": "direct"}
{"buggy": "const getProductInfo = () => {\n  id: 'P001',\n  name: 'Laptop Bag'\n};\nconsole.log(getProductInfo());", "fixed": "const getProductInfo = () => ({\n  id: 'P001',\n  name: 'Laptop Bag'\n});\nconsole.log(getProductInfo());", "category": "arrow_functions", "subcategory": "implicit_return_object_literal", "explanation": "When an arrow function tries to return an object literal using the concise syntax (without `return` and a block body), there's a special rule. If you wrap the object directly in curly braces `{}` like this, JavaScript thinks you're creating a code block, not an object. To tell JavaScript that you want to implicitly return an object, you must wrap the object literal itself in parentheses `({})`. This makes it clear that the curly braces are for the object, not a code block.", "explanation_style": "direct"}
{"buggy": "const isAdult = age => return age >= 18;\nconsole.log(isAdult(20));", "fixed": "const isAdult = age => age >= 18;\nconsole.log(isAdult(20));", "category": "arrow_functions", "subcategory": "return_in_concise_body", "explanation": "You've written an arrow function that checks if `age` is 18 or more. When you remove the curly braces {} from an arrow function, what happens to the expression on the right side of `=>`? Does JavaScript automatically return it, or do you still need to use the `return` keyword there?", "explanation_style": "socratic"}
{"buggy": "displayInfo();\nconst displayInfo = () => {\n  const message = 'Today is a great day!';\n  console.log(message);\n};\n", "fixed": "const displayInfo = () => {\n  const message = 'Today is a great day!';\n  console.log(message);\n};\ndisplayInfo();\n", "category": "arrow_functions", "subcategory": "not_hoisted_like_function", "explanation": "Remember that `const` variables, including those holding arrow functions, are not accessible before their declaration. This means `displayInfo()` cannot be called until JavaScript has executed the line where `displayInfo` is assigned. To fix this, simply move the call to `displayInfo()` below its definition.", "explanation_style": "direct"}
{"buggy": "const getCurrencySymbol = (countryCode) => {\n  if (countryCode === 'RW') 'RWF';\n  else if (countryCode === 'US') 'USD';\n  else 'N/A';\n};\nconsole.log(getCurrencySymbol('RW'));", "fixed": "const getCurrencySymbol = (countryCode) => {\n  if (countryCode === 'RW') return 'RWF';\n  else if (countryCode === 'US') return 'USD';\n  else return 'N/A';\n};\nconsole.log(getCurrencySymbol('RW'));", "category": "arrow_functions", "subcategory": "block_body_missing_return", "explanation": "You're using a block body (with curly braces) for your arrow function, which is good for conditional logic. However, inside a block body, you must explicitly use the `return` keyword to send a value back from the function. Without `return`, the function will execute the `if/else if/else` statements but ultimately return `undefined` to the caller. Add `return` before each currency symbol.", "explanation_style": "direct"}
{"buggy": "const combineText = text1, text2 => `${text1} - ${text2}`;\nconsole.log(combineText('Hello', 'World'));", "fixed": "const combineText = (text1, text2) => `${text1} - ${text2}`;\nconsole.log(combineText('Hello', 'World'));", "category": "arrow_functions", "subcategory": "missing_parentheses_multi_params", "explanation": "Take a close look at how you've defined the parameters `text1` and `text2` for your arrow function. When an arrow function takes more than one input, what special syntax does JavaScript expect around those parameter names?", "explanation_style": "socratic"}
{"buggy": "const getStudentReport = () => {\n  studentName: 'Mugisha Sarah',\n  score: 85,\n  grade: 'A'\n};\nconsole.log(getStudentReport());", "fixed": "const getStudentReport = () => ({\n  studentName: 'Mugisha Sarah',\n  score: 85,\n  grade: 'A'\n});\nconsole.log(getStudentReport());", "category": "arrow_functions", "subcategory": "implicit_return_object_literal", "explanation": "You're trying to return an object with `studentName`, `score`, and `grade` directly from your arrow function using the concise syntax. However, JavaScript sees the initial curly brace `{` and thinks you're starting a function *block*, not an object. When it then sees `studentName: 'Mugisha Sarah',`, it's confused by the colon outside of an object literal context. To fix this, you need to wrap the entire object literal in parentheses `()` to explicitly tell JavaScript that you intend to return an object.", "explanation_style": "direct"}
{"buggy": "const calculateGramsFromKilos = kilos => return kilos * 1000;\nconsole.log(calculateGramsFromKilos(2));", "fixed": "const calculateGramsFromKilos = kilos => kilos * 1000;\nconsole.log(calculateGramsFromKilos(2));", "category": "arrow_functions", "subcategory": "return_in_concise_body", "explanation": "When an arrow function has a 'concise body' (meaning no curly braces {}), it automatically returns the result of the single expression. You don't need the return keyword. In fact, if you use return without curly braces, JavaScript gets confused and throws a SyntaxError. To fix this, simply remove the return keyword, or add curly braces around the body and keep return.", "explanation_style": "direct"}
{"buggy": "console.log(createWelcome('Alice'));\nconst createWelcome = (name) => `Hello, ${name}! Welcome to the platform.`;", "fixed": "const createWelcome = (name) => `Hello, ${name}! Welcome to the platform.`;\nconsole.log(createWelcome('Alice'));", "category": "arrow_functions", "subcategory": "not_hoisted_like_function", "explanation": "In JavaScript, regular function declarations (like function greet() {}) are 'hoisted,' meaning they are available throughout their scope, even before their definition. However, arrow functions assigned to `const` variables are not hoisted. You must define them before you try to call them. When you try to use `createWelcome` before it's declared, JavaScript doesn't know what it is yet, leading to a ReferenceError.", "explanation_style": "direct"}
{"buggy": "const calculateTotalWithTax = (price, taxRate) => {\n  price + (price * taxRate);\n};\nconsole.log(calculateTotalWithTax(100, 0.05));", "fixed": "const calculateTotalWithTax = (price, taxRate) => {\n  return price + (price * taxRate);\n};\nconsole.log(calculateTotalWithTax(100, 0.05));", "category": "arrow_functions", "subcategory": "block_body_missing_return", "explanation": "Look at the curly braces {} around your calculation. What does JavaScript usually expect when you use curly braces in a function body? Does it automatically know you want to send back the result of the last line inside those braces, or do you need to give it a specific instruction to return a value?", "explanation_style": "socratic"}
{"buggy": "const findAverage = score1, score2 => (score1 + score2) / 2;\nconsole.log(findAverage(80, 90));", "fixed": "const findAverage = (score1, score2) => (score1 + score2) / 2;\nconsole.log(findAverage(80, 90));", "category": "arrow_functions", "subcategory": "missing_parentheses_multi_params", "explanation": "When an arrow function takes only one parameter, you can write it without parentheses, like `num => num * 2`. But if your function needs two or more parameters, JavaScript requires you to wrap all of them in parentheses. For example, `(param1, param2) => ...`. Without them, JavaScript cannot understand the function definition and will show a SyntaxError.", "explanation_style": "direct"}
{"buggy": "const getStudentInfo = () => {\n  studentName: 'Uwase Aline',\n  studentId: 'TVET12345'\n};\nconsole.log(getStudentInfo());", "fixed": "const getStudentInfo = () => ({\n  studentName: 'Uwase Aline',\n  studentId: 'TVET12345'\n});\nconsole.log(getStudentInfo());", "category": "arrow_functions", "subcategory": "implicit_return_object_literal", "explanation": "When an arrow function uses the concise syntax (no explicit `return` and no outer curly braces for a block body), and you want to return an object literal, you must wrap the object literal in parentheses `()`. If you just use `{}` without `()`, JavaScript thinks you're starting a code block, not an object. When it sees `studentName:`, it thinks it's a label, and then the comma `,` causes a SyntaxError because labels shouldn't have commas after them in this context. Adding `()` tells JavaScript, 'This curly brace is an object, please return it!'.", "explanation_style": "direct"}
{"buggy": "const generateStatusMessage = (user) => return `User ${user} is online`;\nconsole.log(generateStatusMessage('Jean'));", "fixed": "const generateStatusMessage = (user) => `User ${user} is online`;\nconsole.log(generateStatusMessage('Jean'));", "category": "arrow_functions", "subcategory": "return_in_concise_body", "explanation": "Take a close look at the structure of your `generateStatusMessage` arrow function. You're using a 'concise body' (no curly braces {}). Do you remember what happens automatically when an arrow function has a concise body? Does it need the `return` keyword, or does it implicitly send back the result of the expression?", "explanation_style": "socratic"}
{"buggy": "console.log(celsiusToFahrenheit(25));\nconst celsiusToFahrenheit = (celsius) => (celsius * 9/5) + 32;", "fixed": "const celsiusToFahrenheit = (celsius) => (celsius * 9/5) + 32;\nconsole.log(celsiusToFahrenheit(25));", "category": "arrow_functions", "subcategory": "not_hoisted_like_function", "explanation": "Remember that `const` declarations (and `let` too) are not hoisted in the same way as traditional `function` declarations. This means you cannot use a variable declared with `const` before the line where it is defined. Your `celsiusToFahrenheit` function is an arrow function assigned to a `const` variable, so you need to define it first before you call it. Move the `console.log` line below the `const` declaration to fix this.", "explanation_style": "direct"}
{"buggy": "const calculateFinalPrice = (originalPrice, discountPercentage) => {\n  const discountAmount = originalPrice * (discountPercentage / 100);\n  originalPrice - discountAmount;\n};\nconsole.log(calculateFinalPrice(2000, 10));", "fixed": "const calculateFinalPrice = (originalPrice, discountPercentage) => {\n  const discountAmount = originalPrice * (discountPercentage / 100);\n  return originalPrice - discountAmount;\n};\nconsole.log(calculateFinalPrice(2000, 10));", "category": "arrow_functions", "subcategory": "block_body_missing_return", "explanation": "When an arrow function uses a 'block body' (the curly braces {}), it behaves like a regular function: you must explicitly use the `return` keyword to send a value back. If you perform calculations but don't `return` anything, the function will implicitly return `undefined`. Add `return` before `originalPrice - discountAmount` to make sure the final calculated price is actually sent back.", "explanation_style": "direct"}
{"buggy": "const makeDateString = year, month, day => `${day}/${month}/${year}`;\nconsole.log(makeDateString(2023, 11, 25));", "fixed": "const makeDateString = (year, month, day) => `${day}/${month}/${year}`;\nconsole.log(makeDateString(2023, 11, 25));", "category": "arrow_functions", "subcategory": "missing_parentheses_multi_params", "explanation": "Think about how you define parameters for an arrow function. If you have just one parameter, like `num => ...`, you can often leave out the parentheses. But what's the rule when you have multiple parameters, like `year`, `month`, and `day`? How does JavaScript expect you to group them together in the function definition?", "explanation_style": "socratic"}
{"buggy": "const getUserPermissions = () => {\n  canEdit: true,\n  canDelete: false,\n  canView: true\n};\nconsole.log(getUserPermissions());", "fixed": "const getUserPermissions = () => ({\n  canEdit: true,\n  canDelete: false,\n  canView: true\n});\nconsole.log(getUserPermissions());", "category": "arrow_functions", "subcategory": "implicit_return_object_literal", "explanation": "When you want an arrow function to implicitly (automatically) return an object literal using the concise syntax (without `return` and a block body `{}`), you need to wrap the object literal in parentheses `()`. Otherwise, JavaScript sees the `{}` and interprets it as a code block. Inside a code block, `canEdit:` is treated as a label, and the subsequent comma `,` followed by another property like `canDelete:` leads to a SyntaxError because it's not valid syntax for a code block. The parentheses clarify that you intend to return an object.", "explanation_style": "direct"}
{"buggy": "const formatItem = (item, price) => return `${item}: ${price} RWF`;\nconsole.log(formatItem(\"Milk\", 1200));", "fixed": "const formatItem = (item, price) => `${item}: ${price} RWF`;\nconsole.log(formatItem(\"Milk\", 1200));", "category": "arrow_functions", "subcategory": "return_in_concise_body", "explanation": "Arrow functions have two main forms: a 'block body' with curly braces {} where you must use the 'return' keyword, or a 'concise body' without curly braces where the result of the expression is automatically returned. You cannot use the 'return' keyword in a concise body. To fix this, simply remove the 'return' keyword, and JavaScript will automatically return the string.", "explanation_style": "direct"}
{"buggy": "console.log(sendGreeting(\"Aline\"));\nconst sendGreeting = (name) => `Hello, ${name}! Welcome.`;", "fixed": "const sendGreeting = (name) => `Hello, ${name}! Welcome.`;\nconsole.log(sendGreeting(\"Aline\"));", "category": "arrow_functions", "subcategory": "not_hoisted_like_function", "explanation": "In JavaScript, regular 'function declarations' (like `function myFunction() { ... }`) are 'hoisted' to the top of their scope, meaning you can call them before they appear in your code. However, arrow functions stored in 'const' variables (like `const myFunction = () => { ... }`) are not hoisted. You must declare them before you try to use them. Moving the call to `sendGreeting` after its definition will solve this `ReferenceError`.", "explanation_style": "direct"}
{"buggy": "const getPercentage = (value, total) => {\n  (value / total) * 100;\n};\nconsole.log(getPercentage(50, 200));", "fixed": "const getPercentage = (value, total) => {\n  return (value / total) * 100;\n};\nconsole.log(getPercentage(50, 200));", "category": "arrow_functions", "subcategory": "block_body_missing_return", "explanation": "Look at the curly braces around `(value / total) * 100`. When an arrow function uses these curly braces, it creates a 'block body'. Does JavaScript automatically know that you want to send back (return) the result of the calculation inside this block, or does it need you to explicitly tell it using the 'return' keyword?", "explanation_style": "socratic"}
{"buggy": "const combineWords = word1, word2 => word1 + ' ' + word2;\nconsole.log(combineWords('Hello', 'World'));", "fixed": "const combineWords = (word1, word2) => word1 + ' ' + word2;\nconsole.log(combineWords('Hello', 'World'));", "category": "arrow_functions", "subcategory": "missing_parentheses_multi_params", "explanation": "When an arrow function has only one parameter, you can often skip the parentheses around it (e.g., `name => name.toUpperCase()`). However, if your arrow function takes two or more parameters, like `word1` and `word2` here, JavaScript requires you to wrap all the parameters in parentheses. Add parentheses around `word1, word2` to fix this syntax error.", "explanation_style": "direct"}
{"buggy": "const generateStudentReport = (name, score) => { studentName: name, studentScore: score };\nconsole.log(generateStudentReport('Alice', 85));", "fixed": "const generateStudentReport = (name, score) => ({ studentName: name, studentScore: score });\nconsole.log(generateStudentReport('Alice', 85));", "category": "arrow_functions", "subcategory": "implicit_return_object_literal", "explanation": "When an arrow function has a 'concise body' (no curly braces), it automatically returns the result of the single expression. However, curly braces {} are also used to define an object literal. If you try to implicitly return an object literal directly after the arrow, JavaScript gets confused and thinks the braces are defining a code block, not an object. To tell JavaScript that you want to return an object, wrap the entire object literal in parentheses: `({ ... })`.", "explanation_style": "direct"}
{"buggy": "const calculateVATAmount = price => return price * 0.18;\nconsole.log(calculateVATAmount(1000));", "fixed": "const calculateVATAmount = price => price * 0.18;\nconsole.log(calculateVATAmount(1000));", "category": "arrow_functions", "subcategory": "return_in_concise_body", "explanation": "Arrow functions can have a 'concise body' (no curly braces) for a single expression, where the result is automatically returned. Or they can have a 'block body' (with curly braces) where you must explicitly use `return`. Can you use the `return` keyword in a concise body that doesn't have curly braces, or is `return` only for block bodies?", "explanation_style": "socratic"}
{"buggy": "console.log(`User age: ${calculateUserAge(2005)} years`);\nconst calculateUserAge = (birthYear) => new Date().getFullYear() - birthYear;", "fixed": "const calculateUserAge = (birthYear) => new Date().getFullYear() - birthYear;\nconsole.log(`User age: ${calculateUserAge(2005)} years`);", "category": "arrow_functions", "subcategory": "not_hoisted_like_function", "explanation": "You're trying to use `calculateUserAge` before it has been defined. While regular `function` declarations are available throughout their scope (this is called 'hoisting'), arrow functions assigned to `const` variables are not. They are only available after their declaration. You need to move the `console.log` statement to appear after the `const calculateUserAge` line to fix this `ReferenceError`.", "explanation_style": "direct"}
{"buggy": "const products = [{ name: 'Book', price: 5000 }, { name: 'Pen', price: 500 }, { name: 'Bag', price: 15000 }];\nconst filterByPrice = (list, minPrice) => {\n  list.filter(item => item.price >= minPrice);\n};\nconsole.log(filterByPrice(products, 2000));", "fixed": "const products = [{ name: 'Book', price: 5000 }, { name: 'Pen', price: 500 }, { name: 'Bag', price: 15000 }];\nconst filterByPrice = (list, minPrice) => {\n  return list.filter(item => item.price >= minPrice);\n};\nconsole.log(filterByPrice(products, 2000));", "category": "arrow_functions", "subcategory": "block_body_missing_return", "explanation": "When an arrow function uses curly braces `{}` for its 'block body', you must explicitly use the `return` keyword if you want the function to send back a value. In this code, `list.filter(...)` correctly creates a new filtered array, but because `return` is missing, the `filterByPrice` function silently returns `undefined`. Add `return` before `list.filter(...)` to make it work as intended.", "explanation_style": "direct"}
{"buggy": "const calculateItemTotal = price, quantity => price * quantity;\nconsole.log(calculateItemTotal(2500, 3));", "fixed": "const calculateItemTotal = (price, quantity) => price * quantity;\nconsole.log(calculateItemTotal(2500, 3));", "category": "arrow_functions", "subcategory": "missing_parentheses_multi_params", "explanation": "Arrow functions have special rules for how you define their parameters. If you have only one parameter (like `item => item.price`), you can often omit the parentheses. But what about when you have multiple parameters, like `price` and `quantity` here? Does JavaScript allow you to list them directly, or does it require you to group them in parentheses?", "explanation_style": "socratic"}
{"buggy": "const buildProductConfig = (id, name) => { productId: id, productName: name, isInStock: true };\nconsole.log(buildProductConfig('P001', 'Laptop'));", "fixed": "const buildProductConfig = (id, name) => ({ productId: id, productName: name, isInStock: true });\nconsole.log(buildProductConfig('P001', 'Laptop'));", "category": "arrow_functions", "subcategory": "implicit_return_object_literal", "explanation": "You're trying to implicitly return an object literal directly with curly braces `{}`. However, JavaScript interprets curly braces without an explicit `return` as a 'block body' for statements, not an object literal. When it sees `productId: id`, it thinks `productId` is a label, and then the comma after `id` causes a `SyntaxError`. To implicitly return an object literal, you must wrap the object in parentheses: `({})`.", "explanation_style": "direct"}
{"buggy": "const findMax = num1, num2 => return Math.max(num1, num2);\nconsole.log(findMax(7, 12));", "fixed": "const findMax = (num1, num2) => Math.max(num1, num2);\nconsole.log(findMax(7, 12));", "category": "arrow_functions", "subcategory": "return_in_concise_body", "explanation": "Arrow functions have two main forms: a block body (with curly braces {}) where you must use the 'return' keyword, and a concise body (without curly braces) where the 'return' is implicit. When you use a concise body, you should not write the 'return' keyword explicitly; JavaScript automatically returns the result of the expression. Remove the 'return' keyword to fix this.", "explanation_style": "direct"}
{"buggy": "console.log(getWelcomeMessage('Sandra'));\n\nconst getWelcomeMessage = name => `Muraho, ${name}! Welcome to the system.`;", "fixed": "const getWelcomeMessage = name => `Muraho, ${name}! Welcome to the system.`;\n\nconsole.log(getWelcomeMessage('Sandra'));", "category": "arrow_functions", "subcategory": "not_hoisted_like_function", "explanation": "You've called the 'getWelcomeMessage' function before it has been declared. In JavaScript, regular function declarations are 'hoisted' (moved to the top of their scope) so you can call them anywhere. However, arrow functions stored in 'const' variables, like 'getWelcomeMessage', are not hoisted. You must declare them before you try to use them. Move the function call after its declaration to fix this.", "explanation_style": "direct"}
{"buggy": "const calculateTotalWithTax = amount => {\n  const taxRate = 0.18;\n  amount * (1 + taxRate);\n};\n\nconsole.log(calculateTotalWithTax(1000));", "fixed": "const calculateTotalWithTax = amount => {\n  const taxRate = 0.18;\n  return amount * (1 + taxRate);\n};\n\nconsole.log(calculateTotalWithTax(1000));", "category": "arrow_functions", "subcategory": "block_body_missing_return", "explanation": "You've used curly braces {} for the function body, making it a 'block body'. What does JavaScript do with the result of 'amount * (1 + taxRate);' when it's inside a block body without a 'return' keyword?", "explanation_style": "socratic"}
{"buggy": "const formatFullName = firstName, lastName => `${firstName} ${lastName.toUpperCase()}`;\nconsole.log(formatFullName('Elisa', 'KAYITARE'));", "fixed": "const formatFullName = (firstName, lastName) => `${firstName} ${lastName.toUpperCase()}`;\nconsole.category", "category": "arrow_functions", "subcategory": "missing_parentheses_multi_params", "explanation": "Arrow functions require parentheses around their parameters when there are two or more. If you only have one parameter, like 'name => ...', you can skip the parentheses. But for multiple parameters like 'firstName' and 'lastName', you need to wrap them in parentheses, like '(firstName, lastName) => ...'. Add parentheses around the parameters to fix this syntax error.", "explanation_style": "direct"}
{"buggy": "const getProductDetails = () => {\n  productCode: 'P001',\n  productName: 'Smartphone',\n  price: 250000\n};\n\nconsole.log(getProductDetails());", "fixed": "const getProductDetails = () => ({\n  productCode: 'P001',\n  productName: 'Smartphone',\n  price: 250000\n});\n\nconsole.log(getProductDetails());", "category": "arrow_functions", "subcategory": "implicit_return_object_literal", "explanation": "You're trying to implicitly return an object literal from your arrow function. However, when you use curly braces {} without surrounding them with parentheses, JavaScript interprets them as a code block, not an object literal. This causes a syntax error because 'productName:' is not a valid statement label in a block body. To implicitly return an object literal with multiple properties, you must wrap the object literal in parentheses: '() => ({ key: value })'.", "explanation_style": "direct"}
{"buggy": "const incrementCount = currentCount => return currentCount + 1;\nconsole.log(incrementCount(5));", "fixed": "const incrementCount = currentCount => currentCount + 1;\nconsole.log(incrementCount(5));", "category": "arrow_functions", "subcategory": "return_in_concise_body", "explanation": "You've written 'return' inside a concise arrow function body (one without curly braces). Is the 'return' keyword needed when the function body is just a single expression like 'currentCount + 1'?", "explanation_style": "socratic"}
{"buggy": "console.log(determinePassStatus(75));\n\nconst determinePassStatus = score => score >= 50 ? 'Pass' : 'Fail';", "fixed": "const determinePassStatus = score => score >= 50 ? 'Pass' : 'Fail';\n\nconsole.log(determinePassStatus(75));", "category": "arrow_functions", "subcategory": "not_hoisted_like_function", "explanation": "Remember that arrow functions assigned to 'const' variables, like 'determinePassStatus', behave differently from traditional function declarations. They are not 'hoisted' to the top of your code. This means you cannot call them before they have been defined. Move the line where you call 'determinePassStatus' to after its declaration.", "explanation_style": "direct"}
{"buggy": "const calculateShippingCost = (weight, distance) => {\n  const ratePerKg = 50;\n  const ratePerKm = 10;\n  weight * ratePerKg + distance * ratePerKm;\n};\n\nconsole.log(calculateShippingCost(2, 100));", "fixed": "const calculateShippingCost = (weight, distance) => {\n  const ratePerKg = 50;\n  const ratePerKm = 10;\n  return weight * ratePerKg + distance * ratePerKm;\n};\n\nconsole.log(calculateShippingCost(2, 100));", "category": "arrow_functions", "subcategory": "block_body_missing_return", "explanation": "When an arrow function uses curly braces {} for its body, it's called a 'block body'. Just like a regular function, a block body needs the 'return' keyword if you want it to send a value back. If you don't include 'return', the function will perform the calculation but then silently return 'undefined'. Add 'return' before your calculation to send the correct shipping cost back.", "explanation_style": "direct"}
{"buggy": "const applyDiscount = originalPrice, discountRate => originalPrice * (1 - discountRate);\nconsole.log(applyDiscount(2000, 0.2));", "fixed": "const applyDiscount = (originalPrice, discountRate) => originalPrice * (1 - discountRate);\nconsole.log(applyDiscount(2000, 0.2));", "category": "arrow_functions", "subcategory": "missing_parentheses_multi_params", "explanation": "You have two parameters, 'originalPrice' and 'discountRate', for your arrow function. When an arrow function takes more than one parameter, what must you always put around those parameters in JavaScript?", "explanation_style": "socratic"}
{"buggy": "const createStudentProfile = () => {\n  studentId: 'S005',\n  studentName: 'Uwimana',\n  grade: 'P6'\n};\n\nconsole.log(createStudentProfile());", "fixed": "const createStudentProfile = () => ({\n  studentId: 'S005',\n  studentName: 'Uwimana',\n  grade: 'P6'\n});\n\nconsole.log(createStudentProfile());", "category": "arrow_functions", "subcategory": "implicit_return_object_literal", "explanation": "You are trying to create and return an object literal, but the curly braces {} are being read as a code block, not an object. When an arrow function implicitly returns an object literal, especially one with multiple properties like this, you need to wrap the entire object literal in parentheses. This tells JavaScript that the curly braces are for an object, not a function block, allowing it to be returned correctly.", "explanation_style": "direct"}
{"buggy": "const calculateShipping = weight => return weight * 0.5 + 1000;\nconsole.log(calculateShipping(5));", "fixed": "const calculateShipping = weight => weight * 0.5 + 1000;\nconsole.log(calculateShipping(5));", "category": "arrow_functions", "subcategory": "return_in_concise_body", "explanation": "Arrow functions have two main forms for their body: a 'concise body' (no curly braces) or a 'block body' (with curly braces {}). When you use the concise body, JavaScript automatically returns the result of the expression, so you don't need to write the `return` keyword. The `return` keyword is only used when you have a block body with curly braces, like `weight => { return weight * 0.5 + 1000; }`. Remove the `return` keyword for the concise form.", "explanation_style": "direct"}
{"buggy": "console.log(getStudentGrade('Alice'));\nconst getStudentGrade = (score) => {\n  if (score >= 90) return 'A';\n  if (score >= 80) return 'B';\n  return 'C';\n};\n", "fixed": "const getStudentGrade = (score) => {\n  if (score >= 90) return 'A';\n  if (score >= 80) return 'B';\n  return 'C';\n};\nconsole.log(getStudentGrade('Alice'));\n", "category": "arrow_functions", "subcategory": "not_hoisted_like_function", "explanation": "In JavaScript, regular `function` declarations are 'hoisted,' meaning they are available to be called anywhere in their scope, even before their actual line of code. However, arrow functions assigned to `const` variables are not hoisted. You must declare them before you try to use them. Moving the `console.log` call after the `getStudentGrade` declaration solves this, because the function is then defined before it is accessed.", "explanation_style": "direct"}
{"buggy": "const calculateTotalPrice = (price, quantity) => {\n  price * quantity;\n};\nconsole.log(`Your total is: ${calculateTotalPrice(1500, 3)}`);", "fixed": "const calculateTotalPrice = (price, quantity) => {\n  return price * quantity;\n};\nconsole.log(`Your total is: ${calculateTotalPrice(1500, 3)}`);", "category": "arrow_functions", "subcategory": "block_body_missing_return", "explanation": "When an arrow function uses curly braces `{}` to define its body, it's called a 'block body'. For a block body, do you remember how JavaScript knows what value your function should send back to the place where it was called? What keyword is usually needed inside curly braces to specify the output of a function?", "explanation_style": "socratic"}
{"buggy": "const updateScore = currentScore, points => currentScore + points;\nconsole.log(updateScore(75, 10));", "fixed": "const updateScore = (currentScore, points) => currentScore + points;\nconsole.log(updateScore(75, 10));", "category": "arrow_functions", "subcategory": "missing_parentheses_multi_params", "explanation": "When an arrow function takes only one parameter, you can write it without parentheses, like `score => score * 2`. But if your function needs two or more parameters, JavaScript requires you to wrap all of them inside parentheses. The engine expects `(currentScore, points)` to define the parameters, not `currentScore, points`.", "explanation_style": "direct"}
{"buggy": "const createProductDetails = () => { name: 'Smartphone', price: 250000, inStock: true };\nconsole.log(createProductDetails());", "fixed": "const createProductDetails = () => ({ name: 'Smartphone', price: 250000, inStock: true });\nconsole.log(createProductDetails());", "category": "arrow_functions", "subcategory": "implicit_return_object_literal", "explanation": "You're trying to implicitly return an object literal directly from your arrow function. However, when JavaScript sees curly braces `{}` immediately after the arrow `=>`, it interprets them as the start of a code 'block' (like for `if` statements or `for` loops), not as an object literal. To tell JavaScript that you want to return an object literal using concise syntax, you must wrap the object in parentheses `()`. This makes it clear it's an expression to be returned.", "explanation_style": "direct"}
{"buggy": "const getItemCount = items => return items.length;\nconsole.log(getItemCount([10, 20, 30]));", "fixed": "const getItemCount = items => items.length;\nconsole.log(getItemCount([10, 20, 30]));", "category": "arrow_functions", "subcategory": "return_in_concise_body", "explanation": "Take a closer look at the structure of your arrow function. You have `items => return items.length`. In arrow functions, when do you typically use the `return` keyword, and when does JavaScript automatically return the value of an expression?", "explanation_style": "socratic"}
{"buggy": "console.log(formatId('user123'));\nconst formatId = (id) => `ID: ${id.toUpperCase()}`;\n", "fixed": "const formatId = (id) => `ID: ${id.toUpperCase()}`;\nconsole.log(formatId('user123'));\n", "category": "arrow_functions", "subcategory": "not_hoisted_like_function", "explanation": "When you declare a function using `const` (like `const formatId = ...`), that function behaves like any other `const` variable. It's not available until its declaration line is executed. Trying to call `formatId` before it has been declared will result in a `ReferenceError` because it's in the 'temporal dead zone'. Always declare your `const` arrow functions before you call them.", "explanation_style": "direct"}
{"buggy": "const getDiscountPercentage = (amount) => {\n  if (amount > 10000) {\n    0.15;\n  } else {\n    0.05;\n  }\n};\nconsole.log(`Discount: ${getDiscountPercentage(15000) * 100}%`);", "fixed": "const getDiscountPercentage = (amount) => {\n  if (amount > 10000) {\n    return 0.15;\n  } else {\n    return 0.05;\n  }\n};\nconsole.log(`Discount: ${getDiscountPercentage(15000) * 100}%`);", "category": "arrow_functions", "subcategory": "block_body_missing_return", "explanation": "Your arrow function `getDiscountPercentage` uses curly braces `{}` to create a 'block body'. Inside a block body, JavaScript executes statements, but it doesn't automatically return a value unless you explicitly use the `return` keyword. Even though you have `0.15;` and `0.05;` as expressions, they are not sent back. You need to add `return` before each of those values.", "explanation_style": "direct"}
{"buggy": "const logMessage = type, message => console.log(`[${type.toUpperCase()}]: ${message}`);\nlogMessage('info', 'Application started.');", "fixed": "const logMessage = (type, message) => console.log(`[${type.toUpperCase()}]: ${message}`);\nlogMessage('info', 'Application started.');", "category": "arrow_functions", "subcategory": "missing_parentheses_multi_params", "explanation": "When defining an arrow function, how do you usually group or define multiple input values (parameters) that the function will use?", "explanation_style": "socratic"}
{"buggy": "const getUserPreferences = () => { theme: 'dark', notifications: true, language: 'en' };\nconsole.log(getUserPreferences());", "fixed": "const getUserPreferences = () => ({ theme: 'dark', notifications: true, language: 'en' });\nconsole.log(getUserPreferences());", "category": "arrow_functions", "subcategory": "implicit_return_object_literal", "explanation": "You're trying to return an object literal ` { theme: 'dark', ... }` directly from an arrow function using the 'concise body' syntax. However, JavaScript sees the first `{` and thinks you are starting a code block, not an object. When it then encounters `theme: 'dark'`, it tries to parse `theme:` as a label, and then it gets confused by the comma. To tell JavaScript that the curly braces are for an object and not a code block, you need to wrap the entire object literal in parentheses `()`. This signals that it's an expression to be returned.", "explanation_style": "direct"}
{"buggy": "const getGreetingMessage = (name) => return `Hello, ${name}!`;\nconsole.log(getGreetingMessage('Alice'));", "fixed": "const getGreetingMessage = (name) => `Hello, ${name}!`;\nconsole.log(getGreetingMessage('Alice'));", "category": "arrow_functions", "subcategory": "return_in_concise_body", "explanation": "Arrow functions can have a 'concise body' (without curly braces) or a 'block body' (with curly braces). If you use a concise body, like `name => `Hello, ${name}!` ``, JavaScript automatically returns the result of the expression. The `return` keyword is only needed when you use a block body (`name => { return `Hello, ${name}!` }`). You included `return` in a concise body, which is a syntax error. Remove `return` to fix it.", "explanation_style": "direct"}
{"buggy": "console.log(displayItemPrice('Laptop', 800000));\nconst displayItemPrice = (itemName, price) => {\n  return `${itemName}: ${price} RWF`;\n};", "fixed": "const displayItemPrice = (itemName, price) => {\n  return `${itemName}: ${price} RWF`;\n};\nconsole.log(displayItemPrice('Laptop', 800000));", "category": "arrow_functions", "subcategory": "not_hoisted_like_function", "explanation": "In JavaScript, regular `function` declarations are 'hoisted,' meaning they can be called before they appear in the code. However, arrow functions assigned to `const` variables are not hoisted. They behave like any other `const` variable; you must declare them before you try to use them. Calling `displayItemPrice` before its `const` declaration causes a `ReferenceError` because it hasn't been defined yet. Move the call after the function's declaration to fix this.", "explanation_style": "direct"}
{"buggy": "const calculateShippingFee = (weightKg) => {\n  weightKg * 500;\n};\nconsole.log(calculateShippingFee(5));", "fixed": "const calculateShippingFee = (weightKg) => {\n  return weightKg * 500;\n};\nconsole.log(calculateShippingFee(5));", "category": "arrow_functions", "subcategory": "block_body_missing_return", "explanation": "Look at the curly braces `{}` around `weightKg * 500`. Does JavaScript automatically know you want to return that value when you use braces, or does it think those braces are just wrapping a block of code where you might have multiple steps, and you need to tell it explicitly what to send back?", "explanation_style": "socratic"}
{"buggy": "const calculateTotalWithTax = price, taxRate => price * (1 + taxRate);\nconsole.log(calculateTotalWithTax(100, 0.18));", "fixed": "const calculateTotalWithTax = (price, taxRate) => price * (1 + taxRate);\nconsole.log(calculateTotalWithTax(100, 0.18));", "category": "arrow_functions", "subcategory": "missing_parentheses_multi_params", "explanation": "Arrow functions have a special rule for parameters. If there's only one parameter, you can skip the parentheses, like `name => name.toUpperCase()`. But if you have two or more parameters, you must wrap them in parentheses, like `(price, taxRate)`. Without the parentheses, JavaScript cannot correctly understand `price, taxRate` as multiple parameters, causing a syntax error. Adding parentheses around `price, taxRate` will resolve the problem.", "explanation_style": "direct"}
{"buggy": "const getStudentReport = () => { studentName: 'Mugisha', grade: 'A', status: 'Passed' };\nconsole.log(getStudentReport());", "fixed": "const getStudentReport = () => ({ studentName: 'Mugisha', grade: 'A', status: 'Passed' });\nconsole.g(getStudentReport());", "category": "arrow_functions", "subcategory": "implicit_return_object_literal", "explanation": "You're trying to directly return an object literal using the concise arrow function syntax. When you use curly braces `{}` immediately after `=>`, JavaScript thinks it's a code block, not an object. When it sees properties like `studentName: 'Mugisha'` followed by a comma, it causes a syntax error because it's not valid as a statement within a block. To implicitly return an object literal, you must wrap the object in parentheses: `() => ({ key: value, otherKey: otherValue })`. This tells JavaScript to interpret the `{}` as an object, not a code block.", "explanation_style": "direct"}
{"buggy": "const calculateSquareRoot = num => return Math.sqrt(num);\nconsole.log(calculateSquareRoot(25));", "fixed": "const calculateSquareRoot = num => Math.sqrt(num);\nconsole.log(calculateSquareRoot(25));", "category": "arrow_functions", "subcategory": "return_in_concise_body", "explanation": "Arrow functions allow two styles for their body: a 'concise body' (no curly braces) where the expression's result is implicitly returned, or a 'block body' (with curly braces) where `return` is explicit. You've used the `return` keyword in what looks like a concise body. In this situation, what does JavaScript expect when you don't use curly braces around the function's logic?", "explanation_style": "socratic"}
{"buggy": "console.log(getItemDetails('Milk', 1.5));\nconst getItemDetails = (name, weightKg) => {\n  return `Item: ${name}, Weight: ${weightKg}kg`;\n};", "fixed": "const getItemDetails = (name, weightKg) => {\n  return `Item: ${name}, Weight: ${weightKg}kg`;\n};\nconsole.log(getItemDetails('Milk', 1.5));", "category": "arrow_functions", "subcategory": "not_hoisted_like_function", "explanation": "A common point of confusion is how `const` arrow functions behave compared to traditional `function` declarations. Regular `function` declarations are 'hoisted', meaning you can call them even before their definition in your code. However, `const` arrow functions are not hoisted. They must be defined before you attempt to use them, just like any other variable declared with `const`. The error happens because `getItemDetails` is called before it's created. Move the `console.log` line after the `getItemDetails` definition to fix it.", "explanation_style": "direct"}
{"buggy": "const getDiscountAmount = (price, percentage) => {\n  price * (percentage / 100);\n};\nconsole.log(getDiscountAmount(5000, 10));", "fixed": "const getDiscountAmount = (price, percentage) => price * (percentage / 100);\nconsole.log(getDiscountAmount(5000, 10));", "category": "arrow_functions", "subcategory": "block_body_missing_return", "explanation": "When you use curly braces `{}` in an arrow function, it creates a 'block body'. A block body doesn't automatically return the last value calculated. You need to explicitly use the `return` keyword, like `return price * (percentage / 100);`. Without `return`, the function completes the calculation but silently returns `undefined`. A simpler fix for single-line results is to remove the braces and `return` entirely, using a 'concise body' which implicitly returns the expression's result.", "explanation_style": "direct"}
{"buggy": "const joinWords = word1, word2 => `${word1} ${word2}`;\nconsole.log(joinWords('Hello', 'World'));", "fixed": "const joinWords = (word1, word2) => `${word1} ${word2}`;\nconsole.log(joinWords('Hello', 'World'));", "category": "arrow_functions", "subcategory": "missing_parentheses_multi_params", "explanation": "Think about how you usually define a list of things, like arguments for a function or items in an array, in JavaScript. How do you group those multiple items so JavaScript knows they belong together? This particular error happens because an arrow function needs its multiple parameters to be grouped in a specific way.", "explanation_style": "socratic"}
{"buggy": "const createProductSummary = () => { productId: 'P001', productName: 'Smartphone', stock: 50 };\nconsole.log(createProductSummary());", "fixed": "const createProductSummary = () => ({ productId: 'P001', productName: 'Smartphone', stock: 50 });\nconsole.log(createProductSummary());", "category": "arrow_functions", "subcategory": "implicit_return_object_literal", "explanation": "You're trying to make your arrow function directly return an object with properties like `productId` and `productName`. However, when you use curly braces `{}` right after `=>`, JavaScript interprets them as a code block, not an object literal that you want to return. Inside a code block, `productId: 'P001'` is seen as a label, and the comma after it causes a syntax error because it's not expected in that context. To make JavaScript understand that you want to return an object, you need to wrap the entire object literal in parentheses: `() => ({ key: value })`.", "explanation_style": "direct"}
{"buggy": "const calculateTriple = num => return num * 3;\nconsole.log(calculateTriple(5));", "fixed": "const calculateTriple = num => num * 3;\nconsole.log(calculateTriple(5));", "category": "arrow_functions", "subcategory": "return_in_concise_body", "explanation": "Arrow functions have two main forms: a 'concise body' (without curly braces {}) which automatically returns the value of the expression, and a 'block body' (with curly braces {}) which needs an explicit return keyword. You tried to use `return` inside a concise body, which JavaScript does not allow and causes a SyntaxError. To fix this, simply remove the `return` keyword because the arrow function automatically sends back `num * 3` when there are no curly braces.", "explanation_style": "direct"}
{"buggy": "console.log(sayHi('Aline'));\n\nconst sayHi = name => {\n  return `Muraho, ${name}!`\n};", "fixed": "const sayHi = name => {\n  return `Muraho, ${name}!`\n};\n\nconsole.log(sayHi('Aline'));", "category": "arrow_functions", "subcategory": "not_hoisted_like_function", "explanation": "When you declare an arrow function using `const`, JavaScript treats it like any other `const` variable. This means you can only use it *after* it has been declared in your code. Calling `sayHi('Aline')` before `const sayHi = ...` causes a ReferenceError because `sayHi` hasn't been defined yet. To fix this, always define your `const` arrow functions before you try to call them.", "explanation_style": "direct"}
{"buggy": "const getFormattedPrice = price => {\n  `Rwf ${price.toFixed(2)}`;\n};\nconsole.log(getFormattedPrice(1500));", "fixed": "const getFormattedPrice = price => {\n  return `Rwf ${price.toFixed(2)}`;\n};\nconsole.log(getFormattedPrice(1500));", "category": "arrow_functions", "subcategory": "block_body_missing_return", "explanation": "When an arrow function uses curly braces {} for its body, it becomes a 'block body'. Think of it like a mini-program within the function. Does JavaScript automatically know that the last line of a block body should be the value it sends back? Or does it need you to explicitly tell it what to return?", "explanation_style": "socratic"}
{"buggy": "const addValues = value1, value2 => value1 + value2;\nconsole.log(addValues(10, 20));", "fixed": "const addValues = (value1, value2) => value1 + value2;\nconsole.log(addValues(10, 20));", "category": "arrow_functions", "subcategory": "missing_parentheses_multi_params", "explanation": "For arrow functions, if you have only one parameter (like `num => num * 2`), you can skip the parentheses around it. But if you have two or more parameters (like `value1` and `value2`), JavaScript requires you to wrap them in parentheses: `(value1, value2) => ...`. Without these parentheses, JavaScript gets confused and cannot understand your code, leading to a SyntaxError. Adding the parentheses will solve this problem.", "explanation_style": "direct"}
{"buggy": "const getItemDetails = (id, name) => {\n  itemId: id, itemName: name\n};\nconsole.log(getItemDetails(1, 'Laptop'));", "fixed": "const getItemDetails = (id, name) => ({\n  itemId: id, itemName: name\n});\nconsole.log(getItemDetails(1, 'Laptop'));", "category": "arrow_functions", "subcategory": "implicit_return_object_literal", "explanation": "You're trying to return an object literal directly from an arrow function using concise syntax. When JavaScript sees curly braces `{}` immediately after the arrow, it thinks you're starting a block of code (like a function body) and not an object. When it then sees `itemName: name` after `itemId: id`, the comma between them causes a SyntaxError because it's not expecting another statement like that. To implicitly return an object literal with multiple properties, you must wrap the object literal in parentheses `()` to tell JavaScript it's an expression, not a code block. So, `=> ({ ... })` is the correct way.", "explanation_style": "direct"}
{"buggy": "const multiplyByTen = number => return number * 10;\nconsole.log(multiplyByTen(7));", "fixed": "const multiplyByTen = number => number * 10;\nconsole.log(multiplyByTen(7));", "category": "arrow_functions", "subcategory": "return_in_concise_body", "explanation": "An arrow function without curly braces, like `number => number * 10`, is called a 'concise body'. It's designed to automatically return the result of its single expression. If you add the `return` keyword, you're trying to mix two different ways of defining the function's body. What do you think happens when you combine an automatic return mechanism with an explicit `return` keyword in the same concise line?", "explanation_style": "socratic"}
{"buggy": "console.log(formatItemName('Laptop', 123));\n\nconst formatItemName = (name, id) => {\n  return `${name} (ID: ${id})`\n};", "fixed": "const formatItemName = (name, id) => {\n  return `${name} (ID: ${id})`\n};\n\nconsole.log(formatItemName('Laptop', 123));", "category": "arrow_functions", "subcategory": "not_hoisted_like_function", "explanation": "In JavaScript, regular `function` declarations (like `function myFunction() {}`) are 'hoisted', meaning they can be called before their definition in the code. However, arrow functions defined with `const` (or `let`) are not hoisted. They behave like any other variable. You must declare the `const` variable holding the arrow function *before* you try to use it. Calling `formatItemName` before its `const` declaration leads to a ReferenceError because the variable hasn't been initialized yet.", "explanation_style": "direct"}
{"buggy": "const calculateAreaOfCircle = radius => {\n  Math.PI * radius * radius;\n};\nconsole.log(calculateAreaOfCircle(5));", "fixed": "const calculateAreaOfCircle = radius => {\n  return Math.PI * radius * radius;\n};\nconsole.log(calculateAreaOfCircle(5));", "category": "arrow_functions", "subcategory": "block_body_missing_return", "explanation": "When an arrow function has curly braces `{}` around its body, it's called a 'block body'. In a block body, you need to explicitly tell JavaScript what value to send back using the `return` keyword. Even if you do a calculation, without `return`, the function will just perform the calculation and then silently give `undefined` back to whoever called it. To get the correct area, add `return` before `Math.PI * radius * radius;`.", "explanation_style": "direct"}
{"buggy": "const createGreeting = name, timeOfDay => `Good ${timeOfDay}, ${name}!`;\nconsole.log(createGreeting('Uwase', 'morning'));", "fixed": "const createGreeting = (name, timeOfDay) => `Good ${timeOfDay}, ${name}!`;\nconsole.log(createGreeting('Uwase', 'morning'));", "category": "arrow_functions", "subcategory": "missing_parentheses_multi_params", "explanation": "You're defining an arrow function with two parameters: `name` and `timeOfDay`. For arrow functions, when you have more than one parameter, there's a specific rule about how you write them. If you look closely at how `name, timeOfDay` is written, do you see anything missing that JavaScript usually requires when listing multiple items together, especially in a function's parameter list?", "explanation_style": "socratic"}
{"buggy": "const getStudentReportCard = studentId => {\n  id: studentId, grade: 'A', status: 'Passed'\n};\nconsole.log(getStudentReportCard(101));", "fixed": "const getStudentReportCard = studentId => ({\n  id: studentId, grade: 'A', status: 'Passed'\n});\nconsole.log(getStudentReportCard(101));", "category": "arrow_functions", "subcategory": "implicit_return_object_literal", "explanation": "You intended to have your arrow function implicitly return an object literal. However, when an arrow function body starts with an opening curly brace `{`, JavaScript interprets it as a block of code, not an object literal. Because your object has multiple properties (`id`, `grade`, `status`) separated by commas within what JavaScript thinks is a code block, it leads to a SyntaxError. To tell JavaScript that you want to return an object literal directly, you need to wrap the object literal itself in parentheses: `=> ({ key: value })`. This makes it clear it's an expression to be returned.", "explanation_style": "direct"}
{"buggy": "const calculateSquareValue = num => return num * num;\nconsole.log(calculateSquareValue(7));", "fixed": "const calculateSquareValue = num => num * num;\nconsole.log(calculateSquareValue(7));", "category": "arrow_functions", "subcategory": "return_in_concise_body", "explanation": "Arrow functions have two main forms for their body: a block body (with curly braces {}) or a concise body (without curly braces). If you use a concise body (like num => num * num), the result of the expression is automatically returned, so you don't need the 'return' keyword. The 'return' keyword is only used when you have a block body (num => { return num * num; }). In this case, removing 'return' makes the code correct and concise.", "explanation_style": "direct"}
{"buggy": "console.log(sendGreetingToUser('Uwimana'));\n\nconst sendGreetingToUser = userName => {\n  return `Hello, ${userName}! Welcome.`;\n};", "fixed": "const sendGreetingToUser = userName => {\n  return `Hello, ${userName}! Welcome.`;\n};\n\nconsole.log(sendGreetingToUser('Uwimana'));", "category": "arrow_functions", "subcategory": "not_hoisted_like_function", "explanation": "In JavaScript, regular function declarations (like `function myFunction() { ... }`) are 'hoisted' to the top of their scope, meaning you can call them before they are written in the code. However, arrow functions stored in `const` variables are not hoisted. You must define them *before* you try to call them. Moving the `console.log` statement after the `sendGreetingToUser` declaration fixes this problem.", "explanation_style": "direct"}
{"buggy": "const calculatePercentageOfTotal = (value, total) => {\n  (value / total) * 100;\n};\n\nconsole.log(calculatePercentageOfTotal(30, 200));", "fixed": "const calculatePercentageOfTotal = (value, total) => {\n  return (value / total) * 100;\n};\n\nconsole.log(calculatePercentageOfTotal(30, 200));", "category": "arrow_functions", "subcategory": "block_body_missing_return", "explanation": "Look at the curly braces around (value / total) * 100. When an arrow function uses these curly braces, it's called a 'block body'. What does JavaScript expect you to do inside a block body if you want a value to be sent back to where the function was called?", "explanation_style": "socratic"}
{"buggy": "const sumTwoValues = num1, num2 => num1 + num2;\nconsole.log(sumTwoValues(15, 20));", "fixed": "const sumTwoValues = (num1, num2) => num1 + num2;\nconsole.log(sumTwoValues(15, 20));", "category": "arrow_functions", "subcategory": "missing_parentheses_multi_params", "explanation": "When an arrow function takes one parameter, you can often skip the parentheses around it (e.g., `num => num * 2`). But if your arrow function needs two or more parameters, you must always put parentheses around all of them, like `(param1, param2) => ...`. Without them, JavaScript doesn't understand your function definition.", "explanation_style": "direct"}
{"buggy": "const getProductSpecification = () => {\n  id: 'PROD001',\n  name: 'Laptop',\n  price: 750000\n};\nconsole.log(getProductSpecification());", "fixed": "const getProductSpecification = () => ({\n  id: 'PROD001',\n  name: 'Laptop',\n  price: 750000\n});\nconsole.log(getProductSpecification());", "category": "arrow_functions", "subcategory": "implicit_return_object_literal", "explanation": "When you want an arrow function to directly return an object literal using the concise syntax (without `return` and curly braces), you must wrap the object literal itself in parentheses. If you just use curly braces like `{ id: 'PROD001', ... }`, JavaScript thinks those curly braces are defining a block of code, not an object. The parentheses `()` around the object `{ ... }` tell JavaScript to treat it as an expression to be returned.", "explanation_style": "direct"}
{"buggy": "const incrementValue = x => return x + 5;\nconsole.log(incrementValue(10));", "fixed": "const incrementValue = x => x + 5;\nconsole.log(incrementValue(10));", "category": "arrow_functions", "subcategory": "return_in_concise_body", "explanation": "Consider the two ways to write an arrow function: with a 'block body' (using curly braces like `x => { return x + 5; }`) or with a 'concise body' (no curly braces, like `x => x + 5`). In which of these forms is the `return` keyword automatically implied, and therefore not needed?", "explanation_style": "socratic"}
{"buggy": "console.log(getStudentAverageGrade(85, 90, 78));\n\nconst getStudentAverageGrade = (math, science, history) => {\n  return (math + science + history) / 3;\n};", "fixed": "const getStudentAverageGrade = (math, science, history) => {\n  return (math + science + history) / 3;\n};\n\nconsole.log(getStudentAverageGrade(85, 90, 78));", "category": "arrow_functions", "subcategory": "not_hoisted_like_function", "explanation": "Remember that `const` declarations (which is how we store arrow functions) are not 'hoisted' to the top of your code like traditional `function` declarations. This means you cannot use them before they have been defined. To fix this, simply move the line where you call `getStudentAverageGrade` to appear after its `const` declaration.", "explanation_style": "direct"}
{"buggy": "const formatStudentFullName = (firstName, lastName) => {\n  `${firstName} ${lastName}`;\n};\nconsole.log(formatStudentFullName('Claude', 'Mugisha'));", "fixed": "const formatStudentFullName = (firstName, lastName) => {\n  return `${firstName} ${lastName}`;\n};\nconsole.log(formatStudentFullName('Claude', 'Mugisha'));", "category": "arrow_functions", "subcategory": "block_body_missing_return", "explanation": "When you use curly braces `{}` for the body of an arrow function, it creates a 'block body'. Just like with regular functions, if you want a value to be sent back from a block body, you must explicitly use the `return` keyword. Without `return`, the function will run the code inside, but it will implicitly send `undefined` back to whoever called it.", "explanation_style": "direct"}
{"buggy": "const calculateItemTotalCost = quantity, unitPrice => quantity * unitPrice;\nconsole.log(calculateItemTotalCost(5, 1200));", "fixed": "const calculateItemTotalCost = (quantity, unitPrice) => quantity * unitPrice;\nconsole.log(calculateItemTotalCost(5, 1200));", "category": "arrow_functions", "subcategory": "missing_parentheses_multi_params", "explanation": "When an arrow function has more than one input, like `quantity` and `unitPrice` here, how does JavaScript expect you to group those inputs in the function definition?", "explanation_style": "socratic"}
{"buggy": "const createUserSummary = () => {\n  username: 'patel_k',\n  email: 'patel.k@example.com'\n};\nconsole.log(createUserSummary());", "fixed": "const createUserSummary = () => ({\n  username: 'patel_k',\n  email: 'patel.k@example.com'\n});\nconsole.log(createUserSummary());", "category": "arrow_functions", "subcategory": "implicit_return_object_literal", "explanation": "You're trying to return an object directly from an arrow function. When you use curly braces `{}` without a `return` keyword, JavaScript assumes it's a code block. To tell JavaScript that you want to return an *object literal* implicitly, you need to wrap the object literal itself in parentheses `()`. This makes it an expression that JavaScript knows to return.", "explanation_style": "direct"}
{"buggy": "const calculateFinalAmount = price => return price * 1.05;\nconsole.log(calculateFinalAmount(100));", "fixed": "const calculateFinalAmount = price => price * 1.05;\nconsole.log(calculateFinalAmount(100));", "category": "arrow_functions", "subcategory": "return_in_concise_body", "explanation": "When an arrow function has no curly braces {} around its body (this is called a \"concise body\"), it automatically returns the result of the expression. You do not need and should not use the `return` keyword in this case. Including `return` in a concise body is a syntax error. If you want to use the `return` keyword, you must wrap the function body in curly braces, making it a \"block body\".", "explanation_style": "direct"}
{"buggy": "console.log(sendConfirmation(\"Order #123\"));\n\nconst sendConfirmation = (orderId) => {\n  return `Your order ${orderId} has been confirmed!`;\n};", "fixed": "const sendConfirmation = (orderId) => {\n  return `Your order ${orderId} has been confirmed!`;\n};\n\nconsole.log(sendConfirmation(\"Order #123\"));", "category": "arrow_functions", "subcategory": "not_hoisted_like_function", "explanation": "In JavaScript, regular `function` declarations (like `function myFunction() { ... }`) are 'hoisted,' meaning you can call them before they are written in your code. However, arrow functions stored in `const` variables are not hoisted in the same way. They behave like any other `const` variable and cannot be accessed before they are declared and initialized. Trying to use `sendConfirmation` before its `const` declaration causes a `ReferenceError` because it's not yet defined. To fix this, simply move the call to `sendConfirmation` after its definition.", "explanation_style": "direct"}
