
  # Design EduCode Rwanda Workspace

  This is a code bundle for Design EduCode Rwanda Workspace. The original project is available at https://www.figma.com/design/SKTkwCTVRbRSWsQV9cNLTi/Design-EduCode-Rwanda-Workspace.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  